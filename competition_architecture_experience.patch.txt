diff --git a/.github/agents/security-improver.agent.md b/.github/agents/security-improver.agent.md
index f201fce..eff6b52 100644
--- a/.github/agents/security-improver.agent.md
+++ b/.github/agents/security-improver.agent.md
@@ -279,7 +279,7 @@ Look for:
 
 ## How to execute
 
-### Phase 1 — Security audit
+### Initial — Security audit
 Perform a deep security audit of the codebase.
 
 Create a structured list of findings with:
@@ -302,7 +302,7 @@ Prioritize findings based on:
 - business impact
 - blast radius across users or companies
 
-### Phase 2 — High-value fixes
+### Rules — High-value fixes
 Implement the highest-value security improvements directly in code.
 
 Prioritize:
@@ -322,7 +322,7 @@ Prefer fixes that:
 - improve correctness and auditability
 - align with Odoo security best practices
 
-### Phase 3 — Re-review after fixes
+### Integration — Re-review after fixes
 After the first implementation wave, review the updated codebase again.
 
 Specifically search for:
@@ -337,7 +337,7 @@ Specifically search for:
 Do not stop after the first obvious findings.
 Keep going until the likely remaining issues are minor, speculative, or blocked by missing context.
 
-### Phase 4 — Final hardening summary
+### API — Final hardening summary
 At the end, report:
 1. a concise security audit summary
 2. the concrete fixes implemented
diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index 21ba3f4..a7a5e1b 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -85,9 +85,9 @@ jobs:
 
       - name: Verify portal privilege boundaries
         run: python ci/check_portal_sudo_guard.py
-          python ci/check_portal_v2_ownership.py
-          python ci/check_v2_officiating_contract.py
-          python ci/check_v2_registration_contract.py
+          python ci/check_portal_competition_ownership.py
+          python ci/check_officiating_contract.py
+          python ci/check_registration_contract.py
           python ci/check_access_csv_integrity.py
 
       - name: Verify source collector completeness
@@ -99,19 +99,19 @@ jobs:
       - name: Verify workflow state contracts
         run: python ci/check_workflow_state_contracts.py
 
-      - name: Verify Phase 1 fixture contract
-        run: python ci/check_phase1_fixture_contract.py
+      - name: Verify Fixture ownership contract
+        run: python ci/check_fixture_ownership_contract.py
 
-      - name: Verify Phase 2 rules contract
-        run: python ci/check_phase2_rules_contract.py
+      - name: Verify Rules contract
+        run: python ci/check_rules_contract.py
 
       - name: Verify Phases 3-5 contract
-        run: python ci/check_phases_3_5_contract.py
+        run: python ci/check_competition_pipeline_contract.py
 
-      - name: Verify Phase 5.1 operator handoff
-        run: python ci/check_phase51_schedule_handoff.py
+      - name: Verify operator handoff
+        run: python ci/check_schedule_handoff_contract.py
       - name: Verify Phases 5.1.1 through 5.3 contracts
-        run: python ci/check_phases_511_53_contract.py
+        run: python ci/check_publication_integrity_contract.py
       - name: Report module dependency drift
         run: python ci/check_module_dependency_drift.py
 
diff --git a/.github/workflows/release-candidate.yml b/.github/workflows/release-candidate.yml
index 8d0b493..c63dd47 100644
--- a/.github/workflows/release-candidate.yml
+++ b/.github/workflows/release-candidate.yml
@@ -29,19 +29,20 @@ jobs:
         run: |
           python ci/check_legacy_engine_removed.py
           python ci/check_portal_sudo_guard.py
-          python ci/check_portal_v2_ownership.py
-          python ci/check_v2_officiating_contract.py
-          python ci/check_v2_registration_contract.py
+          python ci/check_portal_competition_ownership.py
+          python ci/check_officiating_contract.py
+          python ci/check_registration_contract.py
           python ci/check_access_csv_integrity.py
           python ci/check_source_collector_contract.py
           python ci/check_addon_integrity.py
           python ci/check_workflow_state_contracts.py
-          python ci/check_phase1_fixture_contract.py
-          python ci/check_phase2_rules_contract.py
-          python ci/check_phases_3_5_contract.py
-          python ci/check_phase51_schedule_handoff.py
-          python ci/check_phases_511_53_contract.py
+          python ci/check_fixture_ownership_contract.py
+          python ci/check_rules_contract.py
+          python ci/check_competition_pipeline_contract.py
+          python ci/check_schedule_handoff_contract.py
+          python ci/check_publication_integrity_contract.py
           python ci/check_doc_freshness.py
+          python ci/check_delivery_language.py
       - run: scripts/ci/run_rc_validation.sh static
 
   odoo-rc:
@@ -92,10 +93,15 @@ jobs:
           pip install -r _odoo/requirements.txt
       - name: Fresh install and module suites
         run: scripts/ci/run_rc_validation.sh install
-      - name: Core and portal suites
+      - name: Upgrade-path smoke
+        run: |
+          DB_NAME=sf_rc_upgrade scripts/ci/run_rc_validation.sh install
+          scripts/ci/run_rc_validation.sh upgrade
+      - name: Core, portal and public suites
         run: |
           scripts/ci/run_rc_validation.sh core
           scripts/ci/run_rc_validation.sh portal
+          scripts/ci/run_rc_validation.sh public
       - name: Upload Odoo logs
         if: always()
         uses: actions/upload-artifact@v4
diff --git a/COMPETITION_ENGINE_V2.md b/COMPETITION_ENGINE.md
similarity index 88%
rename from COMPETITION_ENGINE_V2.md
rename to COMPETITION_ENGINE.md
index 50f0629..524808c 100644
--- a/COMPETITION_ENGINE_V2.md
+++ b/COMPETITION_ENGINE.md
@@ -1,4 +1,4 @@
-# Competition Engine V2
+# Competition Engine
 
 This implementation provides role-owned capabilities and explicit handovers for competition planning and operations.
 
@@ -18,10 +18,10 @@ Registration finalizes a participant set. Format freezes a structure. Calendar p
 
 ## Migration policy
 
-The legacy monolithic engine has been removed from the addon set. New and existing operational flows use the V2 ownership split. Databases upgraded from a release that installed the legacy addon require the normal reviewed upgrade/migration procedure before production rollout; do not silently reinterpret existing production records.
+The legacy monolithic engine has been removed from the addon set. New and existing operational flows use the ownership split. Databases upgraded from a release that installed the legacy addon require the normal reviewed upgrade/migration procedure before production rollout; do not silently reinterpret existing production records.
 
 
-## Phase 5.1 operator handoff
+## Schedule handoff operator handoff
 
 The operator path is fully exposed in the backend: Schedule Planner submits a
 validated revision, Schedule Review Queue provides independent request-changes
diff --git a/CONTEXT.md b/CONTEXT.md
index 98c5220..33cd156 100644
--- a/CONTEXT.md
+++ b/CONTEXT.md
@@ -40,7 +40,7 @@ Developer quick-start (most common tasks)
 1. Add models: `odoo/<module>/models/<file>.py` and export in `models/__init__.py`.
 2. Security: add `security/ir.model.access.csv` and `security/*.xml` record rules.
 3. Views: add `views/*.xml` and register in `__manifest__.py` `data`.
-4. Wizards/services: put transient models in `wizards/` and algorithmic code in the owning V2 module's `services/` package.
+4. Wizards/services: put transient models in `wizards/` and algorithmic code in the owning module's `services/` package.
 5. Tests: put unit/integration tests under `odoo/<module>/tests/` and include at least one focused test for new business logic.
 6. Run module tests: `odoo-bin -d <db> -i <module> --test-enable --stop-after-init`.
 
@@ -81,13 +81,13 @@ Recent additions (2026-05-25)
 - `sports_federation_venues` — venue and playing-area master data used by calendar capacity preparation.
 - `sports_federation_officiating` — referee readiness, availability warnings, and double-booking guards applied to operational schedules.
 - `sports_federation_base/views/menu_items.xml` and related addon menu files — backend navigation now uses journey-first buckets (`Setup`, `Planning`, `Match Day`, `Publication`, `Administration`), with the Planning menu as the primary scheduling entry point.
-- `sports_federation_portal/views/portal_templates.xml` and `portal_v2_templates.xml` — club representatives use the V2 Competition and Match-Day workspaces; direct queues remain secondary operational links.
+- `sports_federation_portal/views/portal_templates.xml` and `competition_templates.xml` — club representatives use the Competition and Match-Day workspaces; direct queues remain secondary operational links.
 - `sports_federation_base`, `sports_federation_tournament`, `sports_federation_rosters`, `sports_federation_result_control`, `sports_federation_standings`, and `sports_federation_public_site` form views now surface inline next-step guidance and direct cross-module handoffs, including season-registration-to-roster, participant-to-roster, result-to-tournament, and tournament-to-standings/publication flows.
-- The V2 competition modules and `sports_federation_portal` expose a clear phase model: scheduling prepares a complete draft, schedule approval publishes an immutable snapshot, match-day operations execute only that snapshot, and portal result pages handle result follow-up.
+- The competition modules and `sports_federation_portal` expose a clear phase model: scheduling prepares a complete draft, schedule approval publishes an immutable snapshot, match-day operations execute only that snapshot, and portal result pages handle result follow-up.
 
-See `odoo/TECHNICAL_NOTE.md` → "V2 competition planning flow" for details.
-See `odoo/TECHNICAL_NOTE.md` → "Guided setup and cross-module handoffs (2026-05-25)" for the Phase 2 intuitiveness baseline.
-See `odoo/TECHNICAL_NOTE.md` → "Canonical planning and match-day flows (2026-05-25)" for the Phase 3 intuitiveness baseline.
+See `odoo/TECHNICAL_NOTE.md` → "competition planning flow" for details.
+See `odoo/TECHNICAL_NOTE.md` → "Guided setup and cross-module handoffs (2026-05-25)" for the guided-setup baseline.
+See `odoo/TECHNICAL_NOTE.md` → "Canonical planning and match-day flows (2026-05-25)" for the planning and match-day baseline.
 
 Recent additions (2026-05-26)
 
diff --git a/CONTRIBUTING.md b/CONTRIBUTING.md
index c70168c..1432006 100644
--- a/CONTRIBUTING.md
+++ b/CONTRIBUTING.md
@@ -5,7 +5,7 @@ This repository expects small, test-backed changes with matching documentation u
 ## Prerequisites
 
 - Python 3.10 or newer for local linting
-- Docker with Compose v2 for the containerized Odoo test runner
+- Docker with Compose current for the containerized Odoo test runner
 - Git Bash, WSL, or another POSIX shell for the `ci/*.sh` scripts on Windows
 
 ## Local setup
@@ -168,7 +168,7 @@ containers/database/filestore/configuration on failure. For fast UI work use
 combined with:
 
 ```bash
-bash ./ci/run_tests.sh --suite v2_competition_contracts
+bash ./ci/run_tests.sh --suite competition_competition_contracts
 ```
 
 ### Pre-push checks
diff --git a/DOCUMENTATION_REVIEW.md b/DOCUMENTATION_REVIEW.md
index 59d97af..71dedd3 100644
--- a/DOCUMENTATION_REVIEW.md
+++ b/DOCUMENTATION_REVIEW.md
@@ -37,7 +37,7 @@ The two portal ownership snapshots duplicate each other, the portal README, ADR-
 
 - Added missing roadmap freshness metadata.
 - Replaced the 100-item roadmap backlog with current priorities and explicit statuses.
-- Marked the V2 competition ownership decomposition as complete for the current release.
+- Marked the competition ownership decomposition as complete for the current release.
 - Added workflow simplification as the primary product priority.
 - Removed the unverified `/web/login` ownership row.
 - Added the tournament-operations JSON-RPC load and action routes.
diff --git a/README.md b/README.md
index c98442f..71b43dc 100644
--- a/README.md
+++ b/README.md
@@ -82,7 +82,7 @@ Quickstart / Installation (example)
 
 Prerequisites
 - Python 3.10+ (for local linting and editor tooling)
-- Docker with Compose v2 (for the containerized Odoo test runner)
+- Docker with Compose current (for the containerized Odoo test runner)
 - PostgreSQL 12+ only if you are running against a separate local Odoo checkout
 - Node.js/npm (optional: for asset tooling)
 - wkhtmltopdf (optional: PDF export)
diff --git a/RELEASE_RUNBOOK.md b/RELEASE_RUNBOOK.md
index 1db0562..a096aea 100644
--- a/RELEASE_RUNBOOK.md
+++ b/RELEASE_RUNBOOK.md
@@ -53,9 +53,9 @@ this runbook:
 ${EDITOR:-vi} addons/RELEASE_RUNBOOK.md
 ```
 
-### Portal V2 controller-load repair evidence
+### Portal controller-load repair evidence
 
-The portal V2 repair removes the stale re-export of the deleted V1 workspace
+The portal repair removes the stale re-export of the deleted V1 workspace
 controller, restores the tournament-registration view in the manifest data
 order, and keeps officiating assignment visibility limited to live current
 publications. No database migration is required; the portal manifest is bumped
@@ -140,7 +140,7 @@ If you need to restrict the release to a subset of modules:
 ./scripts/upgrade_sports_federation.sh --db odoo --modules sports_federation_reporting,sports_federation_portal --dry-run
 ```
 
-### Competition V2 ownership change evidence
+### Competition ownership change evidence
 
 The legacy monolithic competition engine is removed from the addon tree. The
 replacement ownership chain is `sports_federation_competition_core`,
@@ -152,7 +152,7 @@ Before upgrading a database that previously installed the legacy engine:
 
 1. Take the database and filestore backups described above.
 2. Run the upgrade script with `--dry-run` and confirm that the resolved module
-   list contains the V2 chain and does not contain the removed addon.
+   list contains the chain and does not contain the removed addon.
 3. Run the upgrade in a disposable restore of the backup first, then verify
    registration, format freeze, calendar capacity, scheduling, approval
    publication, and match-day operations.
@@ -453,7 +453,7 @@ odoo-bin -d RC_DATABASE \
 
 Verify that publication `matchday_id` values are populated, each match day has
 at most one `live` publication, existing published matches have
-`operational_slot_id = published_slot_id`, and the Phase 5.1.1 to 5.3 contract
+`operational_slot_id = published_slot_id`, and the publication integrity contract
 and module tests pass. The approval migration creates a partial unique index for
 one live publication per match day. Existing data is safe because the preceding
 implementation permitted at most one live publication for the broader edition.
@@ -462,25 +462,25 @@ Before upgrading, take a database backup. Rollback requires restoring that
 backup because the new publication uniqueness semantics and operational audit
 fields are persistent. Do not downgrade only the addon code after operators have
 recorded live deviations.
-## Portal V2 cutover 19.0.4.0.0
+## Portal cutover 19.0.4.0.0
 
-Upgrade `sports_federation_portal` after the V2 competition, registration,
+Upgrade `sports_federation_portal` after the competition, registration,
 schedule-approval and match-day modules. The migration closes open operation
 tasks projected from V1 tournament registrations. It does not delete business
 records.
 
 ```bash
 odoo-bin -d RC_DATABASE -u sports_federation_portal --stop-after-init
-python ci/check_portal_v2_ownership.py
+python ci/check_portal_competition_ownership.py
 python ci/check_portal_sudo_guard.py
 bash ci/run_tests.sh --module sports_federation_portal
 ```
 
 Verify /my/competitions, /my/competition-entries, /my/match-days and one match-day operations page. Confirm that draft schedules and superseded publications do not appear.
 
-Rollback requires a database restore if V2 entries or portal audit events were created after cutover.
+Rollback requires a database restore if entries or portal audit events were created after cutover.
 
-## V2 officials and standalone-match cleanup
+## officials and standalone-match cleanup
 
 Upgrade `sports_federation_tournament`, `sports_federation_officiating`, and
 `sports_federation_portal` together after taking a verified database backup.
@@ -491,14 +491,14 @@ their dependent records.
 odoo-bin -d RC_DATABASE \
   -u sports_federation_tournament,sports_federation_officiating,sports_federation_portal \
   --stop-after-init
-python ci/check_v2_officiating_contract.py
+python ci/check_officiating_contract.py
 ```
 
 Verify that the standalone-match count is zero, create officials through a
 current live match-day publication, and confirm that the official self-service
-portal shows current assignments plus retained V2 history.
+portal shows current assignments plus retained history.
 
-## V2 competition-entry cutover 19.0.5.0.0
+## competition-entry cutover 19.0.5.0.0
 
 Upgrade registration, portal, public site and reporting together after a verified
 backup. The portal migration permanently drops the test-only V1 tournament
@@ -506,6 +506,6 @@ registration table.
 
 ```bash
 odoo-bin -d RC_DATABASE -u sports_federation_registration,sports_federation_portal,sports_federation_public_site,sports_federation_reporting --stop-after-init
-python ci/check_v2_registration_contract.py
+python ci/check_registration_contract.py
 python ci/check_access_csv_integrity.py
 ```
diff --git a/ROADMAP.md b/ROADMAP.md
index 5ca0cf6..074df4c 100644
--- a/ROADMAP.md
+++ b/ROADMAP.md
@@ -35,16 +35,16 @@ Status: In progress
 
 Success criterion: a normal competition can be configured, scheduled, and published without users understanding the underlying Odoo data model.
 
-### P0: Maintain V2 competition ownership boundaries
+### P0: Maintain competition ownership boundaries
 
 Status: Complete for the current release
 
 - Keep registration, format, calendar, scheduling, schedule approval, and
 	match-day operations as explicit handovers.
 - Keep fairness, validation, publication, and operational commands in their
-	owning V2 modules rather than recreating a shared orchestration facade.
+	owning modules rather than recreating a shared orchestration facade.
 - Preserve immutable published snapshots, audit events, and optimistic
-	concurrency guards while extending the V2 flow.
+	concurrency guards while extending the flow.
 
 ### P0: Release and migration confidence
 
diff --git a/ROUTE_INVENTORY.md b/ROUTE_INVENTORY.md
index f236dfc..5f15ffc 100644
--- a/ROUTE_INVENTORY.md
+++ b/ROUTE_INVENTORY.md
@@ -113,13 +113,13 @@ A route change must include:
 
 The previous `/web/login` entry was removed from this inventory because no matching `FederationWebsiteLogin.web_login` implementation was present in the reviewed source snapshot. Re-add it only with an implementation and smoke coverage.
 
-## Portal V2 competition routes
+## Portal competition routes
 
 - `GET /my/competitions`: edition-based competition workspace.
 - `GET /my/competitions/<edition_id>`: represented-team competition detail.
 - `GET /my/match-days`: match days selected through current live publications.
 - `GET /my/match-days/<matchday_id>`: club-scoped match-day detail.
-- `POST /my/competition-entries/new`: V2 competition entry through the portal privilege boundary.
+- `POST /my/competition-entries/new`: competition entry through the portal privilege boundary.
 - `GET /sports/match-days/<matchday_id>/operations`: publication-scoped operations board.
 - `POST /sports/match-days/<matchday_id>/operations/data`: current live operations payload.
 - `POST /sports/match-days/<matchday_id>/matches/<match_id>/action`: match action constrained to that publication.
diff --git a/TECHNICAL_NOTE.md b/TECHNICAL_NOTE.md
index 876e685..89d1a56 100644
--- a/TECHNICAL_NOTE.md
+++ b/TECHNICAL_NOTE.md
@@ -136,9 +136,9 @@ Exception and publication recovery standard:
     portal surfaces now explain when operators should move from one phase to the
     next instead of presenting the routes as equal-weight peers.
 
-## V2 competition planning flow (2026-05-25)
+## competition planning flow (2026-05-25)
 
-The competition workflow is implemented by small, role-owned V2 addons rather
+The competition workflow is implemented by small, role-owned addons rather
 than a monolithic workspace service. Each handover is explicit and the server
 remains authoritative for validation and publication.
 
@@ -393,7 +393,7 @@ helper module instead of repeating raw string tuples in each model.
 
 ## Executive summary
 
-The project is a modular suite of Odoo 19 addons implementing a sports federation management system. Modules are intentionally small and focused: `sports_federation_base` owns master data (clubs, teams, seasons), `sports_federation_tournament` implements tournament structure and match records, and the V2 competition addons own registration, format, calendar, scheduling, approval, and match-day operations. Domain features (`people`, `rosters`, `officiating`, `result_control`, `standings`, `public_site`, `notifications`, `reporting`) extend core behaviour without mixing responsibilities.
+The project is a modular suite of Odoo 19 addons implementing a sports federation management system. Modules are intentionally small and focused: `sports_federation_base` owns master data (clubs, teams, seasons), `sports_federation_tournament` implements tournament structure and match records, and the competition addons own registration, format, calendar, scheduling, approval, and match-day operations. Domain features (`people`, `rosters`, `officiating`, `result_control`, `standings`, `public_site`, `notifications`, `reporting`) extend core behaviour without mixing responsibilities.
 
 Design goals
 
@@ -700,6 +700,6 @@ Related docs and entry points
 
 - High-level context: [odoo/CONTEXT.md](odoo/CONTEXT.md#L1)
 - Workflows (authoritative): [odoo/_workflows/WORKFLOW_TOURNAMENT_LIFECYCLE.md](odoo/_workflows/WORKFLOW_TOURNAMENT_LIFECYCLE.md#L1), [odoo/_workflows/WORKFLOW_MATCH_DAY_OPERATIONS.md](odoo/_workflows/WORKFLOW_MATCH_DAY_OPERATIONS.md#L1), [odoo/_workflows/WORKFLOW_RESULT_PIPELINE.md](odoo/_workflows/WORKFLOW_RESULT_PIPELINE.md#L1)
-- V2 competition architecture: [odoo/COMPETITION_ENGINE_V2.md](odoo/COMPETITION_ENGINE_V2.md#L1)
+- competition architecture: [odoo/COMPETITION_ENGINE.md](odoo/COMPETITION_ENGINE.md#L1)
 
 If you'd like, I can now: (1) open a PR draft for this change, (2) run a repo scan of `README` and `INSTALL_LOG` files and fold additional implementation notes into this document, or (3) run the tests for a selected module to validate there are no immediate regressions.
diff --git a/TESTING_GUIDE.md b/TESTING_GUIDE.md
index 45b39f9..5fb351f 100644
--- a/TESTING_GUIDE.md
+++ b/TESTING_GUIDE.md
@@ -190,9 +190,9 @@ These are the current CI budgets for the most sensitive read paths:
 | `federation.report.schedule._build_season_portfolio_rows()` | 3 queries |
 | `federation.report.schedule._build_club_performance_rows()` | 4 queries |
 | `federation.report.snapshot._compliance_pending_total()` | 1 aggregate query |
-| V2 scheduling validation smoke | ≤ 25 seconds |
+| scheduling validation smoke | ≤ 25 seconds |
 
-The authoritative tests are in the V2 competition, public-site, portal,
+The authoritative tests are in the competition, public-site, portal,
 reporting, and domain test suites. Scheduling regressions belong in
 `sports_federation_scheduling/tests/` and calendar regressions belong in
 `sports_federation_calendar/tests/`.
diff --git a/_workflows/WORKFLOW_TOURNAMENT_LIFECYCLE.md b/_workflows/WORKFLOW_TOURNAMENT_LIFECYCLE.md
index 6ac1317..c97f5b3 100644
--- a/_workflows/WORKFLOW_TOURNAMENT_LIFECYCLE.md
+++ b/_workflows/WORKFLOW_TOURNAMENT_LIFECYCLE.md
@@ -131,7 +131,7 @@ Bulk enrolment is available via the **Import Tournament Participants** wizard.
 `sports_federation_scheduling`, `sports_federation_schedule_approval`, and
 `sports_federation_matchday`
 
-The V2 path is an explicit sequence of handovers:
+The path is an explicit sequence of handovers:
 
 - Freeze the logical structure and stage graph in `sports_federation_format`.
 - Prepare physical match days and playable capacity in
diff --git a/ci/check_ci_hygiene.py b/ci/check_ci_hygiene.py
index 77e3a2a..10f521d 100644
--- a/ci/check_ci_hygiene.py
+++ b/ci/check_ci_hygiene.py
@@ -13,10 +13,10 @@ LEGACY_COMPOSE_DIR = CI_DIR / "legacy" / "compose"
 ACTIVE_COMPOSE = {"docker-compose.ci.yaml"}
 KNOWN_LEGACY_COMPOSE = {
     "docker-compose.override.yaml",
-    "docker-compose.phase3_officiating.yaml",
-    "docker-compose.public_site.phase2.yaml",
-    "docker-compose.result_control.phase2.yaml",
-    "docker-compose.rosters.phase2.yaml",
+    "docker-compose.integration_officiating.yaml",
+    "docker-compose.public_site.legacy.yaml",
+    "docker-compose.result_control.legacy.yaml",
+    "docker-compose.rosters.legacy.yaml",
     "docker-compose.sf_engine.yaml",
     "docker-compose.temp.yaml",
     "docker-compose.temp_task.yaml",
diff --git a/ci/check_phases_3_5_contract.py b/ci/check_competition_pipeline_contract.py
similarity index 100%
rename from ci/check_phases_3_5_contract.py
rename to ci/check_competition_pipeline_contract.py
diff --git a/ci/check_delivery_language.py b/ci/check_delivery_language.py
new file mode 100755
index 0000000..613091d
--- /dev/null
+++ b/ci/check_delivery_language.py
@@ -0,0 +1,14 @@
+#!/usr/bin/env python3
+from pathlib import Path
+import re, sys
+ROOT=Path(__file__).resolve().parents[1]
+version=re.compile(r'(?<![A-Za-z0-9])'+'V'+r'2(?![A-Za-z0-9])',re.I)
+delivery=re.compile(r'\b(?:phase|package)[\s_.-]*\d',re.I)
+errors=[]
+for p in ROOT.rglob('*'):
+    if p.is_file() and '.git' not in p.parts and p.suffix in {'.py','.xml','.md','.json','.yaml','.yml','.sh','.js','.scss'}:
+        for n,line in enumerate(p.read_text().splitlines(),1):
+            if version.search(line) or delivery.search(line): errors.append(f'{p.relative_to(ROOT)}:{n}: {line.strip()}')
+if errors:
+    print('Obsolete delivery language found:\n- '+'\n- '.join(errors),file=sys.stderr);raise SystemExit(1)
+print('Delivery language check passed.')
diff --git a/ci/check_phase1_fixture_contract.py b/ci/check_fixture_ownership_contract.py
similarity index 93%
rename from ci/check_phase1_fixture_contract.py
rename to ci/check_fixture_ownership_contract.py
index 6de0644..9a21e05 100644
--- a/ci/check_phase1_fixture_contract.py
+++ b/ci/check_fixture_ownership_contract.py
@@ -39,8 +39,8 @@ for token in ('"home_score": 1', '"away_score": 1'):
     if token in engine:
         errors.append(f"fabricated bye result remains: {token}")
 if errors:
-    print("Phase 1 fixture contract failed:")
+    print("Fixture ownership contract failed:")
     for error in errors:
         print(f"- {error}")
     sys.exit(1)
-print("Phase 1 fixture contract passed.")
+print("Fixture ownership contract passed.")
diff --git a/ci/check_v2_officiating_contract.py b/ci/check_officiating_contract.py
similarity index 91%
rename from ci/check_v2_officiating_contract.py
rename to ci/check_officiating_contract.py
index 0b4054c..eae32e2 100644
--- a/ci/check_v2_officiating_contract.py
+++ b/ci/check_officiating_contract.py
@@ -9,12 +9,12 @@ required = {
         "logical_fixture_id",
         "matchday_id",
         "publication_id",
-        "_assert_v2_matches",
+        "_assert_competition_matches",
     ],
     "sports_federation_officiating/models/federation_match_club_referee_duty.py": [
         "logical_fixture_id",
         "matchday_id",
-        "_assert_v2_matches",
+        "_assert_competition_matches",
     ],
     "sports_federation_officiating/wizards/federation_matchday_assign_wizard.py": [
         "current_publication_id",
@@ -48,8 +48,8 @@ for obsolete in (
     if (ROOT / obsolete).exists():
         errors.append(f"obsolete V1 artifact remains: {obsolete}")
 if errors:
-    print("V2 officiating contract failed:")
+    print("Officiating contract failed:")
     for error in errors:
         print(f"- {error}")
     sys.exit(1)
-print("V2 officiating and legacy-match contract passed.")
+print("Officiating and legacy-match contract passed.")
diff --git a/ci/check_portal_v2_ownership.py b/ci/check_portal_competition_ownership.py
similarity index 81%
rename from ci/check_portal_v2_ownership.py
rename to ci/check_portal_competition_ownership.py
index c3f5f78..1b658f7 100644
--- a/ci/check_portal_v2_ownership.py
+++ b/ci/check_portal_competition_ownership.py
@@ -4,7 +4,7 @@ from pathlib import Path
 import sys
 
 ROOT = Path(__file__).resolve().parents[1]
-contract = json.loads((ROOT / "ci/contracts/portal_v2_ownership.json").read_text())
+contract = json.loads((ROOT / "ci/contracts/portal_competition_ownership.json").read_text())
 required = {
     "competition": "federation.competition.edition",
     "participation": "federation.participant.set",
@@ -16,18 +16,18 @@ for key, model in required.items():
     if contract["portal_aggregates"].get(key) != model:
         errors.append(f"ownership mismatch for {key}")
 checks = {
-    "sports_federation_portal/models/portal_v2_queries.py": [
+    "sports_federation_portal/models/competition_queries.py": [
         "federation.portal.competition.queries",
         "federation.portal.matchday.queries",
         "current_publication_id",
         '("current_publication_id.state", "=", "live")',
     ],
-    "sports_federation_portal/controllers/portal_v2.py": [
+    "sports_federation_portal/controllers/competition_portal.py": [
         "/my/competitions",
         "/my/match-days",
         "/sports/match-days/<int:matchday_id>/operations",
     ],
-    "sports_federation_portal/views/portal_v2_templates.xml": [
+    "sports_federation_portal/views/competition_templates.xml": [
         "portal_my_competitions",
         "portal_my_matchdays",
         "portal_matchday_operations_page",
@@ -51,8 +51,8 @@ for removed in (
     if removed in manifest:
         errors.append(f"legacy portal asset remains active: {removed}")
 if errors:
-    print("Portal V2 ownership contract failed:")
+    print("Portal ownership contract failed:")
     for error in errors:
         print(f"- {error}")
     sys.exit(1)
-print("Portal V2 ownership contract passed.")
+print("Portal ownership contract passed.")
diff --git a/ci/check_public_v2_competition_contract.py b/ci/check_public_competition_contract.py
similarity index 74%
rename from ci/check_public_v2_competition_contract.py
rename to ci/check_public_competition_contract.py
index 05d8022..befd74c 100644
--- a/ci/check_public_v2_competition_contract.py
+++ b/ci/check_public_competition_contract.py
@@ -4,7 +4,7 @@ import sys
 
 ROOT = Path(__file__).resolve().parents[1]
 controller = (
-    ROOT / "sports_federation_public_site/controllers/public_v2_competitions.py"
+    ROOT / "sports_federation_public_site/controllers/public_competitions_current.py"
 ).read_text()
 queries = (
     ROOT / "sports_federation_public_site/services/public_competition_queries.py"
@@ -14,17 +14,17 @@ schedule = (
 ).read_text()
 manifest = (ROOT / "sports_federation_public_site/__manifest__.py").read_text()
 public_templates = (
-    ROOT / "sports_federation_public_site/views/website_v2_competition_templates.xml"
+    ROOT / "sports_federation_public_site/views/website_competition_templates.xml"
 ).read_text()
 portal_templates = (
-    ROOT / "sports_federation_portal/views/portal_v2_templates.xml"
+    ROOT / "sports_federation_portal/views/competition_templates.xml"
 ).read_text()
 legacy_controller = (
     ROOT / "sports_federation_public_site/controllers/public_competitions.py"
 ).read_text()
 checks = {
     "edition publication boundary": '("website_published", "=", True)' in queries,
-    "V2 division ownership": "division.edition_id == edition" in queries,
+    "division ownership": "division.edition_id == edition" in queries,
     "live publication boundary": '("current_publication_id.state", "=", "live")'
     in queries + controller,
     "immutable publication match boundary": '("schedule_publication_id", "=", publication.id)'
@@ -42,9 +42,9 @@ checks = {
         not in legacy_controller
     ),
     "gameday route": "/gamedays/<int:matchday_id>" in controller,
-    "no implementation generation in public copy": " V2 " not in public_templates,
-    "no implementation generation in portal copy": " V2 " not in portal_templates,
-    "V2 dependencies": all(
+    "no implementation generation in public copy": " " not in public_templates,
+    "no implementation generation in portal copy": " " not in portal_templates,
+    "current dependencies": all(
         module in manifest
         for module in (
             "sports_federation_competition_core",
@@ -58,7 +58,7 @@ checks = {
 failed = [name for name, passed in checks.items() if not passed]
 if failed:
     print(
-        "Public V2 competition contract failed: " + ", ".join(failed), file=sys.stderr
+        "Public competition contract failed: " + ", ".join(failed), file=sys.stderr
     )
     raise SystemExit(1)
-print("Public V2 competition contract passed.")
+print("Public competition contract passed.")
diff --git a/ci/check_phases_511_53_contract.py b/ci/check_publication_integrity_contract.py
similarity index 100%
rename from ci/check_phases_511_53_contract.py
rename to ci/check_publication_integrity_contract.py
diff --git a/ci/check_v2_registration_contract.py b/ci/check_registration_contract.py
similarity index 93%
rename from ci/check_v2_registration_contract.py
rename to ci/check_registration_contract.py
index b93247b..a9d2885 100644
--- a/ci/check_v2_registration_contract.py
+++ b/ci/check_registration_contract.py
@@ -48,6 +48,6 @@ for p in ROOT.glob("sports_federation_*/**/*"):
     ):
         errors.append(f"active V1 registration reference: {p.relative_to(ROOT)}")
 if errors:
-    print("V2 registration contract failed:\n- " + "\n- ".join(errors))
+    print("Registration contract failed:\n- " + "\n- ".join(errors))
     sys.exit(1)
-print("V2 registration ownership contract passed.")
+print("Registration ownership contract passed.")
diff --git a/ci/check_phase2_rules_contract.py b/ci/check_rules_contract.py
similarity index 93%
rename from ci/check_phase2_rules_contract.py
rename to ci/check_rules_contract.py
index c7643db..7f27797 100644
--- a/ci/check_phase2_rules_contract.py
+++ b/ci/check_rules_contract.py
@@ -36,8 +36,8 @@ for token in (
     if token in legacy:
         errors.append(f"Format still hard-codes scoring: {token}")
 if errors:
-    print("Phase 2 rules contract failed:")
+    print("Rules contract failed:")
     for error in errors:
         print(f"- {error}")
     sys.exit(1)
-print("Phase 2 rules contract passed.")
+print("Rules contract passed.")
diff --git a/ci/check_phase51_schedule_handoff.py b/ci/check_schedule_handoff_contract.py
similarity index 94%
rename from ci/check_phase51_schedule_handoff.py
rename to ci/check_schedule_handoff_contract.py
index bf4b4f2..fb618f3 100644
--- a/ci/check_phase51_schedule_handoff.py
+++ b/ci/check_schedule_handoff_contract.py
@@ -57,8 +57,8 @@ for direct_service in ("approval.commands.approve", "approval.commands.publish")
         errors.append(f"approval view bypasses model command wrapper: {direct_service}")
 
 if errors:
-    print("Phase 5.1 schedule handoff contract failed:")
+    print("Schedule handoff contract failed:")
     for error in errors:
         print(f"- {error}")
     sys.exit(1)
-print("Phase 5.1 schedule handoff contract passed.")
+print("Schedule handoff contract passed.")
diff --git a/ci/contracts/portal_v2_ownership.json b/ci/contracts/portal_competition_ownership.json
similarity index 100%
rename from ci/contracts/portal_v2_ownership.json
rename to ci/contracts/portal_competition_ownership.json
diff --git a/ci/contracts/route_inventory.json b/ci/contracts/route_inventory.json
index e836304..14a8960 100644
--- a/ci/contracts/route_inventory.json
+++ b/ci/contracts/route_inventory.json
@@ -12,25 +12,25 @@
       "method": "GET",
       "path": "/my/competitions",
       "owner_module": "sports_federation_portal",
-      "coverage_key": "portal_v2_competitions"
+      "coverage_key": "portal_competition_competitions"
     },
     {
       "method": "GET",
       "path": "/my/match-days",
       "owner_module": "sports_federation_portal",
-      "coverage_key": "portal_v2_matchdays"
+      "coverage_key": "portal_competition_matchdays"
     },
     {
       "method": "POST",
       "path": "/my/competition-entries/new",
       "owner_module": "sports_federation_portal",
-      "coverage_key": "portal_v2_entry_create"
+      "coverage_key": "portal_competition_entry_create"
     },
     {
       "method": "GET",
       "path": "/sports/match-days/<id>/operations",
       "owner_module": "sports_federation_portal",
-      "coverage_key": "portal_v2_matchday_operations"
+      "coverage_key": "portal_competition_matchday_operations"
     },
     {
       "method": "POST",
diff --git a/ci/legacy/compose/docker-compose.public_site.phase2.yaml b/ci/legacy/compose/docker-compose.public_site.workflow.yaml
similarity index 87%
rename from ci/legacy/compose/docker-compose.public_site.phase2.yaml
rename to ci/legacy/compose/docker-compose.public_site.workflow.yaml
index 1be04df..ecbfa3f 100644
--- a/ci/legacy/compose/docker-compose.public_site.phase2.yaml
+++ b/ci/legacy/compose/docker-compose.public_site.workflow.yaml
@@ -12,4 +12,4 @@ services:
       CI_ODOO_DB_PORT: 5432
     volumes:
       - /home/cedric/odoo/addons/ci/test_config.conf:/etc/odoo/odoo.conf:ro
-    command: ["--no-http", "--stop-after-init", "--test-enable", "-d", "test_public_site_phase2", "-i", "sports_federation_public_site"]
+    command: ["--no-http", "--stop-after-init", "--test-enable", "-d", "test_public_site_legacy", "-i", "sports_federation_public_site"]
diff --git a/ci/legacy/compose/docker-compose.result_control.phase2.yaml b/ci/legacy/compose/docker-compose.result_control.workflow.yaml
similarity index 87%
rename from ci/legacy/compose/docker-compose.result_control.phase2.yaml
rename to ci/legacy/compose/docker-compose.result_control.workflow.yaml
index b968dd9..c2317d6 100644
--- a/ci/legacy/compose/docker-compose.result_control.phase2.yaml
+++ b/ci/legacy/compose/docker-compose.result_control.workflow.yaml
@@ -12,4 +12,4 @@ services:
       CI_ODOO_DB_PORT: 5432
     volumes:
       - /home/cedric/odoo/addons/ci/test_config.conf:/etc/odoo/odoo.conf:ro
-    command: ["--no-http", "--stop-after-init", "--test-enable", "-d", "test_result_control_phase2", "-i", "sports_federation_result_control"]
+    command: ["--no-http", "--stop-after-init", "--test-enable", "-d", "test_result_control_legacy", "-i", "sports_federation_result_control"]
diff --git a/ci/legacy/compose/docker-compose.rosters.phase2.yaml b/ci/legacy/compose/docker-compose.rosters.workflow.yaml
similarity index 86%
rename from ci/legacy/compose/docker-compose.rosters.phase2.yaml
rename to ci/legacy/compose/docker-compose.rosters.workflow.yaml
index 65322cf..fb2808e 100644
--- a/ci/legacy/compose/docker-compose.rosters.phase2.yaml
+++ b/ci/legacy/compose/docker-compose.rosters.workflow.yaml
@@ -12,4 +12,4 @@ services:
       CI_ODOO_DB_PORT: 5432
     volumes:
       - /home/cedric/odoo/addons/ci/test_config.conf:/etc/odoo/odoo.conf:ro
-    command: ["--no-http", "--stop-after-init", "--test-enable", "-d", "test_rosters_phase2", "-i", "sports_federation_rosters"]
+    command: ["--no-http", "--stop-after-init", "--test-enable", "-d", "test_rosters_legacy", "-i", "sports_federation_rosters"]
diff --git a/ci/run_tests.sh b/ci/run_tests.sh
index 3dac455..3e4407a 100644
--- a/ci/run_tests.sh
+++ b/ci/run_tests.sh
@@ -12,7 +12,7 @@
 #   bash ci/run_tests.sh --list-suites
 #   bash ci/run_tests.sh --keep                  # keep containers for debugging
 #
-# Requirements: docker compose v2
+# Requirements: docker compose current
 # ──────────────────────────────────────────────────────────────────────
 set -euo pipefail
 
@@ -699,4 +699,4 @@ fi
 
 exit "$EXIT_CODE"
 
-python ci/check_public_v2_competition_contract.py
+python ci/check_public_competition_contract.py
diff --git a/docs/README.md b/docs/README.md
index 051cd31..c3622f7 100644
--- a/docs/README.md
+++ b/docs/README.md
@@ -43,4 +43,4 @@ roadmap or a standalone review snapshot for routine changes.
 
 ## Engineering contracts
 
-- [Phase 0 source-truth contract](phase0_source_truth.md)
+- [Foundation source-truth contract](repository_source_truth.md)
diff --git a/docs/competition_engine_ownership.md b/docs/competition_engine_ownership.md
index e2a55a1..620a8ce 100644
--- a/docs/competition_engine_ownership.md
+++ b/docs/competition_engine_ownership.md
@@ -1,6 +1,6 @@
-# V2 Competition Workflow Ownership
+# Competition Workflow Ownership
 
-The V2 ownership chain is the supported competition-planning architecture. No
+The ownership chain is the supported competition-planning architecture. No
 production data migration is required solely for this ownership clarification.
 
 ## Capability ownership
diff --git a/docs/v2_officiating_and_match_cleanup.md b/docs/officiating_and_match_ownership.md
similarity index 76%
rename from docs/v2_officiating_and_match_cleanup.md
rename to docs/officiating_and_match_ownership.md
index 1ef7820..48bd37f 100644
--- a/docs/v2_officiating_and_match_cleanup.md
+++ b/docs/officiating_and_match_ownership.md
@@ -1,7 +1,7 @@
-# V2 officiating and standalone-match cleanup
+# Officiating and standalone-match cleanup
 
-V2 keeps `federation.match` as the operational execution and result record. A
-valid V2 operational match is backed one-to-one by `federation.fixture` through
+current keeps `federation.match` as the operational execution and result record. A
+valid operational match is backed one-to-one by `federation.fixture` through
 `logical_fixture_id`. Referee assignments, club duties, match sheets and result
 control therefore remain attached to `federation.match`.
 
@@ -14,7 +14,7 @@ publication.
 The `sports_federation_tournament` 19.0.1.4.0 migration deletes test-only
 standalone matches where `logical_fixture_id IS NULL`. It discovers installed
 foreign keys and removes dependent records before deleting those matches.
-Shared clubs, teams, players, referees, seasons, venues and V2 fixtures are not
+Shared clubs, teams, players, referees, seasons, venues and fixtures are not
 removed.
 
 Before upgrading, take a database backup and record the diagnostic count:
diff --git a/docs/p0_p2_verification_checklist.md b/docs/p0_p2_verification_checklist.md
index 40ab067..16e8a0a 100644
--- a/docs/p0_p2_verification_checklist.md
+++ b/docs/p0_p2_verification_checklist.md
@@ -1,12 +1,12 @@
 # P0-P2 Verification Checklist
 
 ## Installation and removal
-- Confirm a fresh database installs the complete V2 chain without the legacy addon.
+- Confirm a fresh database installs the complete chain without the legacy addon.
 - Confirm an existing test database can uninstall/delete the legacy addon without dangling external IDs.
 - Confirm no menu, action, asset, test scope, or documentation points to the removed planner implementation.
 
 ## Ownership and data flow
-- Verify V2 fixtures materialize operational matches exactly once.
+- Verify fixtures materialize operational matches exactly once.
 - Verify Result Control is the only score lifecycle and Format Studio consumes only approved results.
 - Verify configured points and tie-break rules produce expected frozen standings.
 - Verify participant-set replacement invalidates only draft downstream artifacts.
@@ -36,12 +36,12 @@
 - Verify no silent exception path reports success.
 
 ## CI and release
-- Run addon integrity, V2 readiness, workflow contracts, static checks, and full Odoo tests.
+- Run addon integrity, readiness, workflow contracts, static checks, and full Odoo tests.
 - Verify all module manifests, assets, ACLs, routes and migrations.
 - Run a production-like Belgian Championship simulation end to end.
 - Re-run documentation-to-code and release-candidate reviews after all checks pass.
 ## Removed planner-extension replacement
-- [ ] Integrate referee availability, double-booking, certification and club-duty overlap into V2 schedule validation.
-- [ ] Integrate venue opening, court suitability and playing-area constraints into V2 schedule validation.
+- [ ] Integrate referee availability, double-booking, certification and club-duty overlap into schedule validation.
+- [ ] Integrate venue opening, court suitability and playing-area constraints into schedule validation.
 - [ ] Block schedule approval/publication when required officiating or venue checks fail.
 - [ ] Add transaction tests before considering functional parity complete.
diff --git a/docs/portal_v2_ownership.md b/docs/portal_competition_ownership.md
similarity index 88%
rename from docs/portal_v2_ownership.md
rename to docs/portal_competition_ownership.md
index bba5f70..6b1109d 100644
--- a/docs/portal_v2_ownership.md
+++ b/docs/portal_competition_ownership.md
@@ -1,6 +1,6 @@
-# Portal V2 ownership
+# Portal ownership
 
-The portal is a projection of V2 competition records. It does not own a second
+The portal is a projection of competition records. It does not own a second
 competition lifecycle and does not fall back to legacy tournament workspaces.
 
 | Portal concern | Authoritative model |
diff --git a/docs/portal_sudo_inventory.json b/docs/portal_sudo_inventory.json
index aeeb467..6ee5e55 100644
--- a/docs/portal_sudo_inventory.json
+++ b/docs/portal_sudo_inventory.json
@@ -120,13 +120,13 @@
     "statement": "request.env[ \"federation.season.registration\" ]._portal_submit_registration_request( request.env[\"federation.season\"].sudo().browse(season_id), request.env[\"federation.team\"].sudo().browse(team_id), notes=notes, user=request.env.user, )"
   },
   {
-    "file": "sports_federation_portal/controllers/portal_v2.py",
+    "file": "sports_federation_portal/controllers/competition_portal.py",
     "function": "portal_create_competition_entry",
     "reason": "adjacent ownership scope check",
     "statement": "team = request.env[\"federation.team\"].sudo().browse(int(team_id)).exists()"
   },
   {
-    "file": "sports_federation_portal/controllers/portal_v2.py",
+    "file": "sports_federation_portal/controllers/competition_portal.py",
     "function": "portal_create_competition_entry",
     "reason": "adjacent ownership scope check",
     "statement": "window = ( request.env[\"federation.registration.window\"] .sudo() .browse(int(window_id)) .exists() )"
diff --git a/docs/v2_registration_cutover.md b/docs/registration_ownership.md
similarity index 68%
rename from docs/v2_registration_cutover.md
rename to docs/registration_ownership.md
index d28d8f0..f5874c9 100644
--- a/docs/v2_registration_cutover.md
+++ b/docs/registration_ownership.md
@@ -1,10 +1,10 @@
-# V2 registration cutover
+# Registration cutover
 
 The authoritative chain is `federation.registration.window` →
 `federation.competition.entry` → `federation.participant.set`. Public
-registration resolves the open V2 window for the selected division and enforces
+registration resolves the open window for the selected division and enforces
 club ownership, eligibility, duplicate-entry and capacity rules.
 
 The portal 19.0.5.0.0 migration drops the test-only V1 tournament-registration
 table. Take a verified backup before upgrading. Federation staff review entries
-through **Competition Engine V2 → Registration Desk**.
+through **Competition Engine → Registration Desk**.
diff --git a/docs/release/COMPETITION_ACCEPTANCE.md b/docs/release/COMPETITION_ACCEPTANCE.md
new file mode 100644
index 0000000..5b5e355
--- /dev/null
+++ b/docs/release/COMPETITION_ACCEPTANCE.md
@@ -0,0 +1,3 @@
+# Competition Release Acceptance
+
+Record the candidate commit, fresh-install result, restored-database upgrade, public and portal checks, backup restore, and human approvals. Validate registration, format and fixtures, calendar and scheduling, approval and publication, match-day operations, results and standings, access isolation, mobile usability, and rollback. Critical or high defects block release.
diff --git a/docs/release/PERFORMANCE_QUALIFICATION.md b/docs/release/PERFORMANCE_QUALIFICATION.md
new file mode 100644
index 0000000..ff236e1
--- /dev/null
+++ b/docs/release/PERFORMANCE_QUALIFICATION.md
@@ -0,0 +1,3 @@
+# Competition Performance Qualification
+
+Measure graph generation, materialization, fairness scheduling, validation, standings recomputation, portal query counts, public match-day latency, memory, and deterministic repeatability for 8-team, 24-team, and 64-team datasets. Introduce budgets only after stable measurements on representative infrastructure.
diff --git a/docs/release/RC_APPROVAL_POLICY.md b/docs/release/RC_APPROVAL_POLICY.md
index 8402b34..94888da 100644
--- a/docs/release/RC_APPROVAL_POLICY.md
+++ b/docs/release/RC_APPROVAL_POLICY.md
@@ -4,9 +4,9 @@ An RC may be tagged only after all automated gates pass and the approvals below
 
 ## Mandatory automated evidence
 
-- Fresh installation of the V2 competition stack and affected integration addons
+- Fresh installation of the competition stack and affected integration addons
 - Upgrade of all modules on the same database
-- Complete V2 backend and portal suites
+- Complete backend and portal suites
 - Deterministic stage-graph, calendar-timeline, fairness, officiating,
   result-control, and notification contracts
 - Static Python, XML, JavaScript, and whitespace checks
diff --git a/docs/phase0_source_truth.md b/docs/workflow_source_truth.md
similarity index 89%
rename from docs/phase0_source_truth.md
rename to docs/workflow_source_truth.md
index 37a98c2..aa64d78 100644
--- a/docs/phase0_source_truth.md
+++ b/docs/workflow_source_truth.md
@@ -1,10 +1,10 @@
-# Phase 0: Repository Source Truth
+# Foundation: Repository Source Truth
 
 Owner: Federation engineering
 Last reviewed: 2026-08-23
 Review cadence: Every release
 
-Phase 0 establishes a trustworthy input boundary for reviews and release checks.
+Foundation establishes a trustworthy input boundary for reviews and release checks.
 
 ## Guarantees
 
diff --git a/removal.sh b/removal.sh
index affd08f..dd548cf 100644
--- a/removal.sh
+++ b/removal.sh
@@ -38,10 +38,10 @@ git diff --check
 
 echo "[5/5] Running focused source contracts"
 python ci/check_access_csv_integrity.py
-python ci/check_v2_registration_contract.py
+python ci/check_registration_contract.py
 python ci/check_addon_integrity.py
-python ci/check_portal_v2_ownership.py
-python ci/check_v2_officiating_contract.py
+python ci/check_portal_competition_ownership.py
+python ci/check_officiating_contract.py
 
-echo "OK: V2 registration cutover package applied."
+echo "OK: Registration cutover package applied."
 git status --short
diff --git a/scripts/ci/run_rc_validation.sh b/scripts/ci/run_rc_validation.sh
index 6e85852..c9968f4 100644
--- a/scripts/ci/run_rc_validation.sh
+++ b/scripts/ci/run_rc_validation.sh
@@ -9,6 +9,7 @@ modules="sports_federation_base,sports_federation_rules,sports_federation_tourna
 odoo_bin="${ODOO_BIN:-$repo_root/_odoo/odoo-bin}"
 addons_path="${ADDONS_PATH:-$repo_root,$repo_root/_odoo/addons}"
 db_name="${DB_NAME:-sf_rc_validation}"
+upgrade_db_name="${UPGRADE_DB_NAME:-sf_rc_upgrade}"
 common=(
   "$odoo_bin" -d "$db_name"
   --db_host="${PGHOST:-127.0.0.1}"
@@ -52,12 +53,27 @@ run_tags() {
   "${common[@]}" -u "$modules" --test-enable --test-tags "$tags"
 }
 
+assert_modules_installed() {
+  local database="$1" installed expected
+  command -v psql >/dev/null 2>&1 || { echo "ERROR: psql is required" >&2; exit 2; }
+  installed="$(PGPASSWORD="${PGPASSWORD:-odoo}" psql --host="${PGHOST:-127.0.0.1}" --port="${PGPORT:-5432}" --username="${PGUSER:-odoo}" --dbname="$database" --tuples-only --no-align --command="SELECT count(*) FROM ir_module_module WHERE name = ANY(string_to_array('$modules', ',')) AND state = 'installed'")"
+  expected="$(awk -F',' '{print NF}' <<<"$modules")"
+  [[ "$installed" == "$expected" ]] || { echo "ERROR: upgrade database has $installed of $expected required modules" >&2; exit 2; }
+}
+run_upgrade() {
+  require_odoo; assert_modules_installed "$upgrade_db_name"
+  local upgrade_common=("${common[@]}"); upgrade_common[2]="$upgrade_db_name"
+  "${upgrade_common[@]}" -u "$modules"
+}
+
 case "$lane" in
   static) static_checks ;;
   install)
     require_odoo
     "${common[@]}" -i "$modules" --test-enable --test-tags '/sports_federation_base,/sports_federation_rules,/sports_federation_tournament'
     ;;
+  upgrade) run_upgrade ;;
+  public) run_tags '/sports_federation_public_site' ;;
   core) run_tags 'sf_competition_core,sf_stage_graph,sf_calendar_slot_timeline,sf_fairness_solver,/sports_federation_officiating,/sports_federation_result_control,/sports_federation_notifications' ;;
   portal) run_tags '/sports_federation_portal,sf_frontend_http,sf_frontend_accessibility,sf_frontend_mobile' ;;
   all)
@@ -67,5 +83,5 @@ case "$lane" in
     run_tags 'sf_competition_core,sf_stage_graph,sf_calendar_slot_timeline,sf_fairness_solver,/sports_federation_officiating,/sports_federation_result_control,/sports_federation_notifications'
     run_tags '/sports_federation_portal,sf_frontend_http,sf_frontend_accessibility,sf_frontend_mobile'
     ;;
-  *) echo "Usage: $0 {static|install|core|portal|all}" >&2; exit 2 ;;
+  *) echo "Usage: $0 {static|install|upgrade|core|portal|public|all}" >&2; exit 2 ;;
 esac
diff --git a/sports_federation_calendar/views/menu.xml b/sports_federation_calendar/views/menu.xml
index 46c5a9b..e6cc640 100644
--- a/sports_federation_calendar/views/menu.xml
+++ b/sports_federation_calendar/views/menu.xml
@@ -1 +1 @@
-<odoo><menuitem id="menu_calendar_planner" name="Calendar Planner" parent="sports_federation_competition_core.menu_competition_engine_v2_root" action="action_calendar_planner" sequence="40"/></odoo>
+<odoo><menuitem id="menu_calendar_planner" name="Calendar Planner" parent="sports_federation_competition_core.menu_competition_engine_competition_root" action="action_calendar_planner" sequence="40"/></odoo>
diff --git a/sports_federation_competition_core/models/competition_overview.py b/sports_federation_competition_core/models/competition_overview.py
index 4034e11..dc50e46 100644
--- a/sports_federation_competition_core/models/competition_overview.py
+++ b/sports_federation_competition_core/models/competition_overview.py
@@ -4,13 +4,13 @@ from odoo import _, api, fields, models
 class FederationCompetitionEdition(models.Model):
     _inherit = "federation.competition.edition"
 
-    role_assignment_count = fields.Integer(compute="_compute_v2_core_counts")
-    event_count = fields.Integer(compute="_compute_v2_core_counts")
+    role_assignment_count = fields.Integer(compute="_compute_core_counts")
+    event_count = fields.Integer(compute="_compute_core_counts")
     workflow_progress = fields.Integer(compute="_compute_workflow_progress")
     workflow_next_action = fields.Char(compute="_compute_workflow_progress")
 
     @api.depends("role_assignment_ids", "event_ids", "engine_state")
-    def _compute_v2_core_counts(self):
+    def _compute_core_counts(self):
         for rec in self:
             rec.role_assignment_count = len(rec.role_assignment_ids)
             rec.event_count = len(rec.event_ids)
@@ -56,7 +56,7 @@ class FederationCompetitionEdition(models.Model):
             else:
                 rec.workflow_next_action = _("Monitor competition operations")
 
-    def action_v2_role_assignments(self):
+    def action_role_assignments(self):
         self.ensure_one()
         return {
             "type": "ir.actions.act_window",
@@ -67,7 +67,7 @@ class FederationCompetitionEdition(models.Model):
             "context": {"default_edition_id": self.id},
         }
 
-    def action_v2_events(self):
+    def action_competition_events(self):
         self.ensure_one()
         return {
             "type": "ir.actions.act_window",
diff --git a/sports_federation_competition_core/views/competition_edition_views.xml b/sports_federation_competition_core/views/competition_edition_views.xml
index beb7f4b..7c4216a 100644
--- a/sports_federation_competition_core/views/competition_edition_views.xml
+++ b/sports_federation_competition_core/views/competition_edition_views.xml
@@ -1,14 +1,14 @@
 <odoo>
-  <record id="competition_edition_form_v2_workflow" model="ir.ui.view">
-    <field name="name">federation.competition.edition.form.v2.workflow</field>
+  <record id="competition_edition_form_workflow" model="ir.ui.view">
+    <field name="name">federation.competition.edition.form.workflow</field>
     <field name="model">federation.competition.edition</field>
     <field name="inherit_id" ref="sports_federation_tournament.view_federation_competition_edition_form"/>
     <field name="arch" type="xml">
       <xpath expr="//div[@name='button_box']" position="inside">
-        <button name="action_v2_role_assignments" type="object" class="oe_stat_button" icon="fa-users">
+        <button name="action_role_assignments" type="object" class="oe_stat_button" icon="fa-users">
           <field name="role_assignment_count" widget="statinfo" string="Responsibilities"/>
         </button>
-        <button name="action_v2_events" type="object" class="oe_stat_button" icon="fa-history">
+        <button name="action_competition_events" type="object" class="oe_stat_button" icon="fa-history">
           <field name="event_count" widget="statinfo" string="History"/>
         </button>
       </xpath>
@@ -19,7 +19,7 @@
         </group>
       </xpath>
       <xpath expr="//notebook" position="inside">
-        <page string="Responsibilities" name="v2_responsibilities">
+        <page string="Responsibilities" name="responsibilities">
           <field name="role_assignment_ids" context="{'default_edition_id': id}">
             <list editable="bottom"><field name="role"/><field name="user_id"/><field name="active"/></list>
           </field>
@@ -27,7 +27,7 @@
       </xpath>
     </field>
   </record>
-  <record id="action_competition_overview_v2" model="ir.actions.act_window">
+  <record id="action_competition_overview" model="ir.actions.act_window">
     <field name="name">Competition Overview</field><field name="res_model">federation.competition.edition</field><field name="view_mode">kanban,list,form</field>
   </record>
 </odoo>
diff --git a/sports_federation_competition_core/views/menu.xml b/sports_federation_competition_core/views/menu.xml
index 36d4547..a4b75d8 100644
--- a/sports_federation_competition_core/views/menu.xml
+++ b/sports_federation_competition_core/views/menu.xml
@@ -1,7 +1,7 @@
 <odoo>
-  <menuitem id="menu_competition_engine_v2_root" name="Competition Workflow" parent="sports_federation_base.menu_federation_root" sequence="25"/>
-  <menuitem id="menu_competition_overview_v2" name="Competition Overview" parent="menu_competition_engine_v2_root" action="action_competition_overview_v2" sequence="10"/>
-  <menuitem id="menu_competition_engine_v2_technical" name="Technical Records" parent="menu_competition_engine_v2_root" sequence="90" groups="sports_federation_base.group_federation_manager"/>
-  <record id="action_competition_roles_v2" model="ir.actions.act_window"><field name="name">Role Assignments</field><field name="res_model">federation.competition.role.assignment</field><field name="view_mode">list,form</field></record>
-  <menuitem id="menu_competition_roles_v2" name="Role Assignments" parent="menu_competition_engine_v2_technical" action="action_competition_roles_v2" sequence="10"/>
+  <menuitem id="menu_competition_engine_competition_root" name="Competition Workflow" parent="sports_federation_base.menu_federation_root" sequence="25"/>
+  <menuitem id="menu_competition_overview_competition" name="Competition Overview" parent="menu_competition_engine_competition_root" action="action_competition_overview" sequence="10"/>
+  <menuitem id="menu_competition_engine_competition_technical" name="Technical Records" parent="menu_competition_engine_competition_root" sequence="90" groups="sports_federation_base.group_federation_manager"/>
+  <record id="action_competition_roles_competition" model="ir.actions.act_window"><field name="name">Role Assignments</field><field name="res_model">federation.competition.role.assignment</field><field name="view_mode">list,form</field></record>
+  <menuitem id="menu_competition_roles_competition" name="Role Assignments" parent="menu_competition_engine_competition_technical" action="action_competition_roles_competition" sequence="10"/>
 </odoo>
diff --git a/sports_federation_finance_bridge/README.md b/sports_federation_finance_bridge/README.md
index fae3489..243e779 100644
--- a/sports_federation_finance_bridge/README.md
+++ b/sports_federation_finance_bridge/README.md
@@ -125,7 +125,7 @@ The finance bridge now auto-creates a finance event when a
 - Reconciliation support: auto-created events also receive a deterministic
   `external_ref` using the fee-type code and source record identity
 
-## Result Approval Finance Hooks (Phase 2)
+## Result Approval Finance Hooks (Rules)
 
 match_result_hooks.py extends `federation.match` with:
 
diff --git a/sports_federation_finance_bridge/tests/__init__.py b/sports_federation_finance_bridge/tests/__init__.py
index 6a13d93..21dbd6a 100644
--- a/sports_federation_finance_bridge/tests/__init__.py
+++ b/sports_federation_finance_bridge/tests/__init__.py
@@ -4,4 +4,4 @@ from . import test_finance_hooks
 from . import test_referee_reimbursement
 from . import test_sanction_finance
 from . import test_season_registration_finance
-from . import test_phase3
+from . import test_integration
diff --git a/sports_federation_finance_bridge/tests/test_finance_hooks.py b/sports_federation_finance_bridge/tests/test_finance_hooks.py
index 2765efe..fb2570b 100644
--- a/sports_federation_finance_bridge/tests/test_finance_hooks.py
+++ b/sports_federation_finance_bridge/tests/test_finance_hooks.py
@@ -1,5 +1,5 @@
 """
-Tests for Phase 2 finance hooks:
+Tests for Rules finance hooks:
 - action_create_venue_finance_event works correctly on a match with a venue
 - On result approval, if result_fee_type_id is set, a finance event is created
 - Multiple matches can each have their own auto finance event on approval
diff --git a/sports_federation_finance_bridge/tests/test_phase3.py b/sports_federation_finance_bridge/tests/test_workflow.py
similarity index 100%
rename from sports_federation_finance_bridge/tests/test_phase3.py
rename to sports_federation_finance_bridge/tests/test_workflow.py
diff --git a/sports_federation_format/services/__init__.py b/sports_federation_format/services/__init__.py
index 4156e75..ab53c6c 100644
--- a/sports_federation_format/services/__init__.py
+++ b/sports_federation_format/services/__init__.py
@@ -1,2 +1,4 @@
 from . import stage_graph_engine
 from . import fixture_materializer
+from . import graph_validator
+from . import round_robin_generator
diff --git a/sports_federation_format/services/graph_validator.py b/sports_federation_format/services/graph_validator.py
new file mode 100644
index 0000000..38be8ea
--- /dev/null
+++ b/sports_federation_format/services/graph_validator.py
@@ -0,0 +1,26 @@
+from odoo import _, api, models
+from odoo.exceptions import ValidationError
+
+class FederationStageGraphValidator(models.AbstractModel):
+    _name = "federation.stage.graph.validator"
+    _description = "Stage Graph Validator"
+
+    @api.model
+    def validate(self, structure):
+        adjacency = {stage.id: [] for stage in structure.stage_ids}
+        progressions = self.env["federation.structure.stage.progression"].search([("structure_id", "=", structure.id), ("active", "=", True)])
+        for progression in progressions:
+            adjacency[progression.source_stage_id.id].append(progression.target_stage_id.id)
+        visiting, completed = set(), set()
+        def visit(stage_id):
+            if stage_id in visiting:
+                raise ValidationError(_("Stage graph contains a cycle."))
+            if stage_id in completed:
+                return
+            visiting.add(stage_id)
+            for target_id in adjacency.get(stage_id, []):
+                visit(target_id)
+            visiting.remove(stage_id); completed.add(stage_id)
+        for stage in structure.stage_ids:
+            visit(stage.id)
+        return True
diff --git a/sports_federation_format/services/round_robin_generator.py b/sports_federation_format/services/round_robin_generator.py
new file mode 100644
index 0000000..eb4e8e3
--- /dev/null
+++ b/sports_federation_format/services/round_robin_generator.py
@@ -0,0 +1,19 @@
+from odoo import api, models
+
+class FederationRoundRobinGenerator(models.AbstractModel):
+    _name = "federation.fixture.generator.round.robin"
+    _description = "Round-Robin Fixture Generator"
+
+    @api.model
+    def generate(self, stage, participants, double=False):
+        teams = list(participants.mapped("team_id")); work = teams + [False] if len(teams) % 2 else teams[:]; rounds = []
+        for round_index in range(len(work) - 1):
+            pairs = []
+            for index in range(len(work) // 2):
+                first, second = work[index], work[-index - 1]
+                if first and second:
+                    pairs.append((first, second) if round_index % 2 == 0 else (second, first))
+            rounds.append(pairs); work = [work[0], work[-1], *work[1:-1]]
+        if double:
+            rounds += [[(away, home) for home, away in pairs] for pairs in rounds]
+        return self.env["federation.fixture"].create([{"structure_id": stage.structure_id.id, "stage_id": stage.id, "round_number": rn, "sequence": seq * 10, "home_team_id": home.id, "away_team_id": away.id} for rn, pairs in enumerate(rounds, 1) for seq, (home, away) in enumerate(pairs, 1)])
diff --git a/sports_federation_format/services/stage_graph_engine.py b/sports_federation_format/services/stage_graph_engine.py
index 89153aa..a266f9d 100644
--- a/sports_federation_format/services/stage_graph_engine.py
+++ b/sports_federation_format/services/stage_graph_engine.py
@@ -10,28 +10,7 @@ class FederationStageGraphEngine(models.AbstractModel):
 
     @api.model
     def validate_graph(self, structure):
-        adj = {s.id: [] for s in structure.stage_ids}
-        visiting = set()
-        done = set()
-        for e in self.env["federation.structure.stage.progression"].search(
-            [("structure_id", "=", structure.id), ("active", "=", True)]
-        ):
-            adj[e.source_stage_id.id].append(e.target_stage_id.id)
-
-        def visit(n):
-            if n in visiting:
-                raise ValidationError(_("Stage graph contains a cycle."))
-            if n in done:
-                return
-            visiting.add(n)
-            for x in adj[n]:
-                visit(x)
-            visiting.remove(n)
-            done.add(n)
-
-        for stage in structure.stage_ids:
-            visit(stage.id)
-        return True
+        return self.env["federation.stage.graph.validator"].validate(structure)
 
     def _participants(self, stage):
         return stage.stage_participant_ids.sorted(lambda p: (p.seed, p.id))
@@ -82,33 +61,7 @@ class FederationStageGraphEngine(models.AbstractModel):
         return True
 
     def _round_robin(self, stage, double):
-        teams = list(self._participants(stage).mapped("team_id"))
-        work = teams + [False] if len(teams) % 2 else teams[:]
-        rounds = []
-        for rnd in range(len(work) - 1):
-            pairs = []
-            for i in range(len(work) // 2):
-                a, b = work[i], work[-i - 1]
-                if a and b:
-                    pairs.append((a, b) if rnd % 2 == 0 else (b, a))
-            rounds.append(pairs)
-            work = [work[0], work[-1], *work[1:-1]]
-        if double:
-            rounds += [[(b, a) for a, b in pairs] for pairs in rounds]
-        self.env["federation.fixture"].create(
-            [
-                {
-                    "structure_id": stage.structure_id.id,
-                    "stage_id": stage.id,
-                    "round_number": rn,
-                    "sequence": seq * 10,
-                    "home_team_id": a.id,
-                    "away_team_id": b.id,
-                }
-                for rn, pairs in enumerate(rounds, 1)
-                for seq, (a, b) in enumerate(pairs, 1)
-            ]
-        )
+        return self.env["federation.fixture.generator.round.robin"].generate(stage, self._participants(stage), double=double)
 
     def _seed_order(self, size):
         order = [1, 2]
diff --git a/sports_federation_format/views/menu.xml b/sports_federation_format/views/menu.xml
index 75ec246..1fc539d 100644
--- a/sports_federation_format/views/menu.xml
+++ b/sports_federation_format/views/menu.xml
@@ -1 +1 @@
-<odoo><menuitem id="menu_format_studio" name="Format Studio" parent="sports_federation_competition_core.menu_competition_engine_v2_root" action="action_format_studio" sequence="30"/></odoo>
+<odoo><menuitem id="menu_format_studio" name="Format Studio" parent="sports_federation_competition_core.menu_competition_engine_competition_root" action="action_format_studio" sequence="30"/></odoo>
diff --git a/sports_federation_import_tools/README.md b/sports_federation_import_tools/README.md
index 8c4efb9..0a39d2a 100644
--- a/sports_federation_import_tools/README.md
+++ b/sports_federation_import_tools/README.md
@@ -113,27 +113,27 @@ Current controller layout:
 Phased Maintainability Plan
 ---------------------------
 
-- Phase 1 completed: the former multi-model gateway hotspot was split into
+- Initial completed: the former multi-model gateway hotspot was split into
   per-model files without changing model names, fields, or table ownership.
   Test impact: existing import-tools tests should stay green. Migration risk:
   none, because this is a file-layout change only.
-- Phase 2 completed: delivery staging, idempotency, workflow handoff, and
+- Rules completed: delivery staging, idempotency, workflow handoff, and
   retention cleanup now live in dedicated helper mixins while
   ``federation.integration.delivery`` keeps the ORM contract stable. Test
   impact: inbound delivery staging, idempotency, and retention coverage stays
   green. Migration risk: replay semantics and attachment linkage.
-- Phase 3 completed: token generation, hashing, verification, migration, and
+- Integration completed: token generation, hashing, verification, migration, and
   rotation or audit helpers now live in dedicated partner mixins while
   ``federation.integration.partner`` keeps the stable ORM entry points for
   authentication and subscription lookup. Test impact: token rotation and
   legacy-token migration coverage stay green. Migration risk: stored hash
   compatibility and audit logging.
-- Phase 4 completed: ``wizards/import_wizard_mixin.py`` now composes dedicated
+- API completed: ``wizards/import_wizard_mixin.py`` now composes dedicated
   CSV and governance or result helper mixins while preserving the shared wizard
   API. Test impact: shared mixin coverage plus wizard-specific dry-run and
   live-import flows stay green. Migration risk: approval checksum binding and
   result-summary compatibility.
-- Phase 5 completed: ``controllers/integration_api.py`` now composes dedicated
+- Operations completed: ``controllers/integration_api.py`` now composes dedicated
   auth and response helper mixins while preserving the public route surface and
   existing headers and status codes. Test impact: integration API credential,
   rate-limit, finance export, and inbound delivery coverage stays green.
diff --git a/sports_federation_matchday/README.md b/sports_federation_matchday/README.md
index ebb9581..4ef13ac 100644
--- a/sports_federation_matchday/README.md
+++ b/sports_federation_matchday/README.md
@@ -20,8 +20,8 @@ approved publication evidence.
 ## Validation
 
 ```bash
-python ci/check_phase51_schedule_handoff.py
-python ci/check_phases_511_53_contract.py
+python ci/check_schedule_handoff_contract.py
+python ci/check_publication_integrity_contract.py
 bash ci/run_tests.sh --module sports_federation_schedule_approval
 bash ci/run_tests.sh --module sports_federation_matchday
 ```
diff --git a/sports_federation_matchday/views/menu.xml b/sports_federation_matchday/views/menu.xml
index a3b4acb..d77d159 100644
--- a/sports_federation_matchday/views/menu.xml
+++ b/sports_federation_matchday/views/menu.xml
@@ -1,5 +1,5 @@
 <odoo>
-  <menuitem id="menu_matchday_control" name="Match-Day Control" parent="sports_federation_competition_core.menu_competition_engine_v2_root" action="action_matchday_control" sequence="70" groups="sports_federation_matchday.group_matchday_manager,sports_federation_base.group_federation_manager"/>
-  <menuitem id="menu_matchday_incidents" name="Incidents" parent="sports_federation_competition_core.menu_competition_engine_v2_technical" action="action_matchday_incidents" sequence="60"/>
-  <menuitem id="menu_matchday_deviations" name="Operational Deviations" parent="sports_federation_competition_core.menu_competition_engine_v2_technical" action="action_matchday_deviations" sequence="61"/>
+  <menuitem id="menu_matchday_control" name="Match-Day Control" parent="sports_federation_competition_core.menu_competition_engine_competition_root" action="action_matchday_control" sequence="70" groups="sports_federation_matchday.group_matchday_manager,sports_federation_base.group_federation_manager"/>
+  <menuitem id="menu_matchday_incidents" name="Incidents" parent="sports_federation_competition_core.menu_competition_engine_competition_technical" action="action_matchday_incidents" sequence="60"/>
+  <menuitem id="menu_matchday_deviations" name="Operational Deviations" parent="sports_federation_competition_core.menu_competition_engine_competition_technical" action="action_matchday_deviations" sequence="61"/>
 </odoo>
diff --git a/sports_federation_officiating/README.md b/sports_federation_officiating/README.md
index 03e121b..fd032c4 100644
--- a/sports_federation_officiating/README.md
+++ b/sports_federation_officiating/README.md
@@ -28,7 +28,7 @@ Officials may only access assignments linked to their portal identity. Club repr
 
 Coverage includes assignment lifecycle, duties, reimbursements, portal access, match-day tours, and workspace integration.
 
-## V2 ownership
+## ownership
 
 Official assignments and club-supplied duties belong to fixture-backed
 operational matches. Use **Officiating → Plan Match-Day Officials** to assign a
diff --git a/sports_federation_officiating/models/federation_match_club_referee_duty.py b/sports_federation_officiating/models/federation_match_club_referee_duty.py
index ef5afc1..30dd7b4 100644
--- a/sports_federation_officiating/models/federation_match_club_referee_duty.py
+++ b/sports_federation_officiating/models/federation_match_club_referee_duty.py
@@ -141,16 +141,16 @@ class FederationMatchClubRefereeDuty(models.Model):
         matches = self.env["federation.match"].browse(
             [vals.get("match_id") for vals in vals_list if vals.get("match_id")]
         )
-        self._assert_v2_matches(matches)
+        self._assert_competition_matches(matches)
         return super().create(vals_list)
 
     @api.model
-    def _assert_v2_matches(self, matches):
+    def _assert_competition_matches(self, matches):
         invalid = matches.filtered(lambda match: not match.logical_fixture_id)
         if invalid:
             raise ValidationError(
                 _(
-                    "Club official duties require V2 fixture-backed matches: %(matches)s",
+                    "Club official duties require fixture-backed matches: %(matches)s",
                     matches=", ".join(invalid.mapped("display_name")),
                 )
             )
@@ -158,7 +158,7 @@ class FederationMatchClubRefereeDuty(models.Model):
 
     def write(self, vals):
         if "match_id" in vals:
-            self._assert_v2_matches(
+            self._assert_competition_matches(
                 self.env["federation.match"].browse(vals.get("match_id"))
             )
         return super().write(vals)
diff --git a/sports_federation_officiating/models/federation_match_referee.py b/sports_federation_officiating/models/federation_match_referee.py
index 8ee92ec..2d4feaa 100644
--- a/sports_federation_officiating/models/federation_match_referee.py
+++ b/sports_federation_officiating/models/federation_match_referee.py
@@ -202,11 +202,11 @@ class FederationMatchReferee(models.Model):
 
     @api.model_create_multi
     def create(self, vals_list):
-        """Create assignments only for V2 fixture-backed operational matches."""
+        """Create assignments only for fixture-backed operational matches."""
         matches = self.env["federation.match"].browse(
             [vals.get("match_id") for vals in vals_list if vals.get("match_id")]
         )
-        self._assert_v2_matches(matches)
+        self._assert_competition_matches(matches)
         records = super().create(vals_list)
         Dispatcher = self.env.get("federation.notification.dispatcher")
         if Dispatcher is not None:
@@ -215,12 +215,12 @@ class FederationMatchReferee(models.Model):
         return records
 
     @api.model
-    def _assert_v2_matches(self, matches):
+    def _assert_competition_matches(self, matches):
         invalid = matches.filtered(lambda match: not match.logical_fixture_id)
         if invalid:
             raise ValidationError(
                 _(
-                    "Officials can only be assigned to V2 fixture-backed matches: %(matches)s",
+                    "Officials can only be assigned to fixture-backed matches: %(matches)s",
                     matches=", ".join(invalid.mapped("display_name")),
                 )
             )
@@ -228,7 +228,7 @@ class FederationMatchReferee(models.Model):
 
     def write(self, vals):
         if "match_id" in vals:
-            self._assert_v2_matches(
+            self._assert_competition_matches(
                 self.env["federation.match"].browse(vals.get("match_id"))
             )
         return super().write(vals)
diff --git a/sports_federation_officiating/tests/__init__.py b/sports_federation_officiating/tests/__init__.py
index 52dcb04..4d8e61c 100644
--- a/sports_federation_officiating/tests/__init__.py
+++ b/sports_federation_officiating/tests/__init__.py
@@ -1,8 +1,8 @@
 from . import test_officiating
 from . import test_tour_match_day
 from . import test_tour_officiating
-from . import test_phase0_officiating
+from . import test_workflow0_officiating
 from . import test_club_referee_duty
 from . import test_tour_club_referee_duty
 from . import test_reimbursement
-from . import test_v2_officiating_contract
+from . import test_competition_officiating_contract
diff --git a/sports_federation_officiating/tests/test_club_referee_duty.py b/sports_federation_officiating/tests/test_club_referee_duty.py
index d129bb4..66ab5e6 100644
--- a/sports_federation_officiating/tests/test_club_referee_duty.py
+++ b/sports_federation_officiating/tests/test_club_referee_duty.py
@@ -1,4 +1,4 @@
-"""Tests for federation.match.club.referee.duty (Phase 5)."""
+"""Tests for federation.match.club.referee.duty (Operations)."""
 
 from datetime import timedelta
 
diff --git a/sports_federation_officiating/tests/test_v2_officiating_contract.py b/sports_federation_officiating/tests/test_competition_officiating_contract.py
similarity index 90%
rename from sports_federation_officiating/tests/test_v2_officiating_contract.py
rename to sports_federation_officiating/tests/test_competition_officiating_contract.py
index 36bb632..5bf74fd 100644
--- a/sports_federation_officiating/tests/test_v2_officiating_contract.py
+++ b/sports_federation_officiating/tests/test_competition_officiating_contract.py
@@ -3,8 +3,8 @@ from odoo.tests import tagged
 from odoo.tests.common import TransactionCase
 
 
-@tagged("post_install", "-at_install", "sf_officiating_v2")
-class TestV2OfficiatingContract(TransactionCase):
+@tagged("post_install", "-at_install", "sf_officiating_competition")
+class TestcurrentOfficiatingContract(TransactionCase):
     def test_legacy_match_cannot_receive_referee_assignment(self):
         legacy_match = self.env["federation.match"].create(
             {
@@ -14,7 +14,7 @@ class TestV2OfficiatingContract(TransactionCase):
                 .id,
             }
         )
-        referee = self.env["federation.referee"].create({"name": "V2 Guard Referee"})
+        referee = self.env["federation.referee"].create({"name": "current Guard Referee"})
         with self.assertRaises(ValidationError):
             self.env["federation.match.referee"].create(
                 {"match_id": legacy_match.id, "referee_id": referee.id, "role": "head"}
diff --git a/sports_federation_officiating/tests/test_phase0_officiating.py b/sports_federation_officiating/tests/test_workflow_officiating.py
similarity index 98%
rename from sports_federation_officiating/tests/test_phase0_officiating.py
rename to sports_federation_officiating/tests/test_workflow_officiating.py
index c588f6c..9945312 100644
--- a/sports_federation_officiating/tests/test_phase0_officiating.py
+++ b/sports_federation_officiating/tests/test_workflow_officiating.py
@@ -1,4 +1,4 @@
-"""Phase 0 officiating tests: conflict detection, inline creation, batch wizard."""
+"""Foundation officiating tests: conflict detection, inline creation, batch wizard."""
 
 from odoo.exceptions import ValidationError
 from odoo.tests.common import TransactionCase
diff --git a/sports_federation_officiating/views/federation_matchday_assign_wizard_views.xml b/sports_federation_officiating/views/federation_matchday_assign_wizard_views.xml
index 5d36908..c40ab99 100644
--- a/sports_federation_officiating/views/federation_matchday_assign_wizard_views.xml
+++ b/sports_federation_officiating/views/federation_matchday_assign_wizard_views.xml
@@ -5,7 +5,7 @@
     <field name="arch" type="xml">
       <form string="Assign Officials to Match Day">
         <sheet>
-          <div class="alert alert-info" role="status">Assignments are created only for V2 fixture-backed matches in the current live publication.</div>
+          <div class="alert alert-info" role="status">Assignments are created only for fixture-backed matches in the current live publication.</div>
           <group>
             <group string="Published match day"><field name="matchday_id"/><field name="assignment_type"/><field name="role"/></group>
             <group string="Responsible official"><field name="referee_id" invisible="assignment_type != 'referee'" required="assignment_type == 'referee'"/><field name="club_id" invisible="assignment_type != 'club'" required="assignment_type == 'club'"/><field name="open_duties" invisible="assignment_type != 'club'"/></group>
diff --git a/sports_federation_portal/README.md b/sports_federation_portal/README.md
index c92f46f..40a2353 100644
--- a/sports_federation_portal/README.md
+++ b/sports_federation_portal/README.md
@@ -58,9 +58,9 @@ The module includes HTTP smoke, ownership, access-denied, team, player, roster,
 
 `PORTAL_OWNERSHIP_COVERAGE.md`, `PORTAL_OWNERSHIP_TEST_MATRIX.md`, and `ROADMAP_RC.md` are deleted. Their durable rules are consolidated here and in ADR-0001; the test files are the authoritative live coverage map.
 
-## V2 competition portal
+## competition portal
 
-The active competition portal is edition-based. Participation comes from V2
+The active competition portal is edition-based. Participation comes from
 competition entries and finalized participant sets. Match-day schedule data is
 rendered only from each match day's current live publication. Draft schedules,
 review evidence and superseded publications are intentionally unavailable.
diff --git a/sports_federation_portal/__manifest__.py b/sports_federation_portal/__manifest__.py
index ec9989b..ced0fba 100644
--- a/sports_federation_portal/__manifest__.py
+++ b/sports_federation_portal/__manifest__.py
@@ -46,7 +46,7 @@ on top of the federation base modules.
         "views/federation_club_representative_portal_views.xml",
         "views/qol_views.xml",
         "views/portal_templates.xml",
-        "views/portal_v2_templates.xml",
+        "views/competition_templates.xml",
         "views/portal_officiating_templates.xml",
         "views/portal_roster_templates.xml",
         "views/portal_result_templates.xml",
diff --git a/sports_federation_portal/controllers/__init__.py b/sports_federation_portal/controllers/__init__.py
index e02e64e..e419473 100644
--- a/sports_federation_portal/controllers/__init__.py
+++ b/sports_federation_portal/controllers/__init__.py
@@ -7,4 +7,4 @@ from . import referee_duty_portal
 from . import operation_tasks
 from . import manager_operation_tasks
 from . import qol
-from . import portal_v2
+from . import portal_competition
diff --git a/sports_federation_portal/controllers/portal_v2.py b/sports_federation_portal/controllers/competition_portal.py
similarity index 98%
rename from sports_federation_portal/controllers/portal_v2.py
rename to sports_federation_portal/controllers/competition_portal.py
index c9b1839..a6d4e99 100644
--- a/sports_federation_portal/controllers/portal_v2.py
+++ b/sports_federation_portal/controllers/competition_portal.py
@@ -6,7 +6,7 @@ from werkzeug.utils import redirect
 from .portal_helpers import FederationPortalBase
 
 
-class FederationPortalV2(FederationPortalBase):
+class FederationPortalcurrent(FederationPortalBase):
     def _not_found_or_denied(self):
         return self._render_access_denied()
 
@@ -50,7 +50,7 @@ class FederationPortalV2(FederationPortalBase):
         website=True,
     )
     def portal_my_competition_entries(self, **kw):
-        teams = request.env["federation.portal.v2.scope"].teams(user=request.env.user)
+        teams = request.env["federation.portal.scope"].teams(user=request.env.user)
         entries = request.env["federation.competition.entry"].search(
             [("team_id", "in", teams.ids)], order="create_date desc,id desc"
         )
@@ -86,7 +86,7 @@ class FederationPortalV2(FederationPortalBase):
                 .exists()
             )
             team = request.env["federation.team"].sudo().browse(int(team_id)).exists()
-            request.env["federation.portal.v2.scope"].assert_team(
+            request.env["federation.portal.scope"].assert_team(
                 team, user=request.env.user
             )
             if not window or window.state != "open":
diff --git a/sports_federation_portal/migrations/19.0.4.0.0/post-migrate.py b/sports_federation_portal/migrations/19.0.4.0.0/post-migrate.py
index 14aaf97..7e0eb09 100644
--- a/sports_federation_portal/migrations/19.0.4.0.0/post-migrate.py
+++ b/sports_federation_portal/migrations/19.0.4.0.0/post-migrate.py
@@ -1,6 +1,6 @@
 def migrate(cr, version):
     # V1 registration tasks are projections, not business records. Close them
-    # so the next sync can create V2 competition-entry tasks without ambiguity.
+    # so the next sync can create competition-entry tasks without ambiguity.
     cr.execute("""
         UPDATE federation_operation_task
            SET state = 'done', completed_on = NOW()
diff --git a/sports_federation_portal/models/__init__.py b/sports_federation_portal/models/__init__.py
index e0d75d3..3a09732 100644
--- a/sports_federation_portal/models/__init__.py
+++ b/sports_federation_portal/models/__init__.py
@@ -13,5 +13,5 @@ from . import federation_match_sheet
 from . import federation_player
 from . import portal_status_labels
 from . import operation_task
-from . import portal_v2_queries
+from . import competition_queries
 from . import federation_competition_entry
diff --git a/sports_federation_portal/models/portal_v2_queries.py b/sports_federation_portal/models/competition_queries.py
similarity index 97%
rename from sports_federation_portal/models/portal_v2_queries.py
rename to sports_federation_portal/models/competition_queries.py
index 5810f9a..3e1783a 100644
--- a/sports_federation_portal/models/portal_v2_queries.py
+++ b/sports_federation_portal/models/competition_queries.py
@@ -2,9 +2,9 @@ from odoo import _, api, fields, models
 from odoo.exceptions import AccessError
 
 
-class FederationPortalV2Scope(models.AbstractModel):
-    _name = "federation.portal.v2.scope"
-    _description = "Portal V2 Scope Service"
+class FederationPortalcurrentScope(models.AbstractModel):
+    _name = "federation.portal.scope"
+    _description = "Portal Scope Service"
 
     @api.model
     def team_domain(self, user=None):
@@ -41,7 +41,7 @@ class FederationPortalCompetitionQueries(models.AbstractModel):
     _description = "Portal Competition Projections"
 
     def _scope(self):
-        return self.env["federation.portal.v2.scope"]
+        return self.env["federation.portal.scope"]
 
     @api.model
     def visible_editions(self, user=None):
@@ -241,10 +241,10 @@ class FederationPortalCompetitionQueries(models.AbstractModel):
 
 class FederationPortalMatchdayQueries(models.AbstractModel):
     _name = "federation.portal.matchday.queries"
-    _description = "Portal V2 Match-Day Projections"
+    _description = "Portal Match-Day Projections"
 
     def _scope(self):
-        return self.env["federation.portal.v2.scope"]
+        return self.env["federation.portal.scope"]
 
     @api.model
     def visible_matchdays(self, user=None):
diff --git a/sports_federation_portal/tests/__init__.py b/sports_federation_portal/tests/__init__.py
index 825b589..93adb12 100644
--- a/sports_federation_portal/tests/__init__.py
+++ b/sports_federation_portal/tests/__init__.py
@@ -15,4 +15,4 @@ from . import test_qol
 
 from . import test_production_security_matrix
 
-from . import test_portal_v2
+from . import test_competition_portal
diff --git a/sports_federation_portal/tests/test_portal_v2.py b/sports_federation_portal/tests/test_competition_portal.py
similarity index 79%
rename from sports_federation_portal/tests/test_portal_v2.py
rename to sports_federation_portal/tests/test_competition_portal.py
index 6739d1f..67d09dc 100644
--- a/sports_federation_portal/tests/test_portal_v2.py
+++ b/sports_federation_portal/tests/test_competition_portal.py
@@ -2,14 +2,14 @@ from odoo.tests import tagged
 from odoo.tests.common import TransactionCase
 
 
-@tagged("post_install", "-at_install", "sf_portal_v2")
-class TestPortalV2Contract(TransactionCase):
-    def test_v2_query_services_are_registered(self):
+@tagged("post_install", "-at_install", "sf_portal_competition")
+class TestPortalcurrentContract(TransactionCase):
+    def test_competition_query_services_are_registered(self):
         self.assertIn("federation.portal.competition.queries", self.env)
         self.assertIn("federation.portal.matchday.queries", self.env)
-        self.assertIn("federation.portal.v2.scope", self.env)
+        self.assertIn("federation.portal.scope", self.env)
 
-    def test_v2_templates_are_loaded(self):
+    def test_competition_templates_are_loaded(self):
         for xmlid in (
             "sports_federation_portal.portal_my_competitions",
             "sports_federation_portal.portal_my_competition_detail",
@@ -27,7 +27,7 @@ class TestPortalV2Contract(TransactionCase):
             )
         )
 
-    def test_portal_manifest_uses_v2_owners(self):
+    def test_portal_manifest_uses_competition_owners(self):
         module = self.env["ir.module.module"].search(
             [("name", "=", "sports_federation_portal")], limit=1
         )
diff --git a/sports_federation_portal/tests/test_template_accessibility.py b/sports_federation_portal/tests/test_template_accessibility.py
index 16b0a6a..d556f48 100644
--- a/sports_federation_portal/tests/test_template_accessibility.py
+++ b/sports_federation_portal/tests/test_template_accessibility.py
@@ -18,8 +18,8 @@ class TestPortalTemplateAccessibility(TransactionCase):
             "portal_roster_templates": etree.parse(
                 str(views_dir / "portal_roster_templates.xml")
             ).getroot(),
-            "portal_v2_templates": etree.parse(
-                str(views_dir / "portal_v2_templates.xml")
+            "competition_templates": etree.parse(
+                str(views_dir / "competition_templates.xml")
             ).getroot(),
             "portal_officiating_templates": etree.parse(
                 str(views_dir / "portal_officiating_templates.xml")
@@ -131,7 +131,7 @@ class TestPortalTemplateAccessibility(TransactionCase):
         self._assert_alert_roles(
             "portal_roster_templates", "portal_my_match_sheet_detail"
         )
-        self._assert_alert_roles("portal_v2_templates", "portal_my_competitions")
+        self._assert_alert_roles("competition_templates", "portal_my_competitions")
         self._assert_alert_roles(
             "portal_officiating_templates", "portal_my_referee_assignments"
         )
@@ -170,7 +170,7 @@ class TestPortalTemplateAccessibility(TransactionCase):
             "portal_officiating_templates", "portal_my_referee_assignments"
         )
         self._assert_tables_have_captions_and_scoped_headers(
-            "portal_v2_templates",
+            "competition_templates",
             "portal_my_competition_detail",
         )
 
diff --git a/sports_federation_portal/tests/test_template_mobile.py b/sports_federation_portal/tests/test_template_mobile.py
index 44de5c6..11425ae 100644
--- a/sports_federation_portal/tests/test_template_mobile.py
+++ b/sports_federation_portal/tests/test_template_mobile.py
@@ -29,8 +29,8 @@ class TestPortalTemplateMobile(TransactionCase):
             "portal_roster_templates": etree.parse(
                 str(views_dir / "portal_roster_templates.xml")
             ).getroot(),
-            "portal_v2_templates": etree.parse(
-                str(views_dir / "portal_v2_templates.xml")
+            "competition_templates": etree.parse(
+                str(views_dir / "competition_templates.xml")
             ).getroot(),
             "portal_officiating_templates": etree.parse(
                 str(views_dir / "portal_officiating_templates.xml")
@@ -185,7 +185,7 @@ class TestPortalTemplateMobile(TransactionCase):
 
     def test_workspace_portal_wide_tables_wrapped(self):
         self._assert_wide_tables_wrapped_responsive(
-            "portal_v2_templates",
+            "competition_templates",
             "portal_my_competition_detail",
         )
 
diff --git a/sports_federation_portal/views/portal_v2_templates.xml b/sports_federation_portal/views/competition_templates.xml
similarity index 62%
rename from sports_federation_portal/views/portal_v2_templates.xml
rename to sports_federation_portal/views/competition_templates.xml
index 1a2c754..3b1d82b 100644
--- a/sports_federation_portal/views/portal_v2_templates.xml
+++ b/sports_federation_portal/views/competition_templates.xml
@@ -4,20 +4,20 @@
       <t t-set="breadcrumbs_searchbar" t-value="True"/>
       <t t-call="portal.portal_searchbar"><t t-set="title">Competitions</t></t>
       <div class="container py-3">
-        <div class="d-flex justify-content-between align-items-center mb-3"><div><h2 class="mb-1">Competition workspace</h2><p class="text-muted mb-0">Competitions containing one of your represented teams are shown.</p></div><a href="/my/match-days" class="btn btn-outline-primary">Match days</a></div>
+        <div class="d-flex justify-content-between align-items-center mb-3"><div><h2 class="mb-1">Your competitions</h2><p class="text-muted mb-0">Everything your teams need for registration, schedules, match days and results.</p></div><a href="/my/match-days" class="btn btn-outline-primary">Match days</a></div>
         <div class="row g-3" t-if="competition_cards">
           <div class="col-12 col-lg-6" t-foreach="competition_cards" t-as="card">
-            <article class="card h-100"><div class="card-body"><div class="d-flex justify-content-between gap-3"><div><h3 class="h5 mb-1"><t t-esc="card['edition'].display_name"/></h3><p class="text-muted mb-2"><t t-esc="', '.join(card['teams'].mapped('display_name'))"/></p></div><span t-attf-class="badge text-bg-#{card['status_tone']}"><t t-esc="card['status_label']"/></span></div><p class="mb-1"><strong>Published match days:</strong> <t t-esc="card['published_matchday_count']"/></p><p class="mb-3"><strong>Result follow-up:</strong> <t t-esc="card['result_follow_up_count']"/></p><a t-attf-href="/my/competitions/#{card['edition'].id}" class="btn btn-primary">Open competition</a></div></article>
+            <article class="card h-100 shadow-sm border-0"><div class="card-body"><div class="d-flex justify-content-between gap-3"><div><h3 class="h5 mb-1"><t t-esc="card['edition'].display_name"/></h3><p class="text-muted mb-2"><t t-esc="', '.join(card['teams'].mapped('display_name'))"/></p></div><span t-attf-class="badge text-bg-#{card['status_tone']}"><t t-esc="card['status_label']"/></span></div><p class="mb-1"><strong>Published match days:</strong> <t t-esc="card['published_matchday_count']"/></p><p class="mb-3"><strong>Result follow-up:</strong> <t t-esc="card['result_follow_up_count']"/></p><a t-attf-href="/my/competitions/#{card['edition'].id}" class="btn btn-primary">Open competition</a></div></article>
           </div>
         </div>
-        <div class="alert alert-info" role="alert" t-else="">No competition currently contains one of your represented teams.</div>
+        <div class="alert alert-info" role="alert" t-else="">No competitions are available yet. Your competitions will appear after a team entry is accepted.</div>
       </div>
     </t>
   </template>
   <template id="portal_my_competition_detail" name="Competition Workspace">
     <t t-call="portal.portal_layout"><div class="container py-3"><a href="/my/competitions" class="text-decoration-none">← Competitions</a><div class="d-flex justify-content-between align-items-start mt-2 mb-4"><div><h2><t t-esc="workspace['edition'].display_name"/></h2><p class="text-muted"><t t-esc="', '.join(workspace['teams'].mapped('display_name'))"/></p></div><span t-attf-class="badge text-bg-#{workspace['status_tone']}"><t t-esc="workspace['status_label']"/></span></div>
       <div class="row g-3"><section class="col-12 col-lg-6"><div class="card h-100"><div class="card-header"><strong>Participation</strong></div><div class="card-body"><div t-foreach="workspace['entries']" t-as="entry" class="mb-2"><t t-esc="entry.team_id.display_name"/> · <span class="badge text-bg-light"><t t-esc="entry.state"/></span></div><div t-foreach="workspace['participant_sets']" t-as="pset"><strong><t t-esc="pset.division_id.display_name"/></strong>: <t t-esc="', '.join(pset.line_ids.filtered(lambda l: l.team_id in workspace['teams']).mapped('team_id.display_name'))"/></div></div></div></section>
-      <section class="col-12 col-lg-6"><div class="card h-100"><div class="card-header"><strong>Upcoming published match days</strong></div><div class="list-group list-group-flush"><a t-foreach="workspace['matchdays']" t-as="day" t-attf-href="/my/match-days/#{day.id}" class="list-group-item list-group-item-action"><strong><t t-esc="day.display_name"/></strong><br/><span class="text-muted"><t t-esc="day.date"/> · <t t-esc="day.venue_id.display_name"/> · publication v<t t-esc="day.current_publication_id.version"/></span></a><div class="list-group-item text-muted" t-if="not workspace['matchdays']">No current publication is available.</div></div></div></section></div>
+      <section class="col-12 col-lg-6"><div class="card h-100"><div class="card-header"><strong>Upcoming published match days</strong></div><div class="list-group list-group-flush"><a t-foreach="workspace['matchdays']" t-as="day" t-attf-href="/my/match-days/#{day.id}" class="list-group-item list-group-item-action"><strong><t t-esc="day.display_name"/></strong><br/><span class="text-muted"><t t-esc="day.date"/> · <t t-esc="day.venue_id.display_name"/> · official schedule revision <t t-esc="day.current_publication_id.version"/></span></a><div class="list-group-item text-muted" t-if="not workspace['matchdays']">No current publication is available.</div></div></div></section></div>
       <section class="card mt-3"><div class="card-header"><strong>Fixtures and results</strong></div><div class="table-responsive"><table class="table mb-0"><caption class="visually-hidden">Fixtures and results</caption><thead><tr><th scope="col">Kickoff</th><th scope="col">Match</th><th scope="col">Published slot</th><th scope="col">Status</th></tr></thead><tbody><tr t-foreach="workspace['matches']" t-as="match"><td><t t-esc="match.date_scheduled"/></td><td><t t-esc="match.display_name"/></td><td><t t-esc="match.published_slot_id.display_name"/></td><td><t t-esc="match.state"/></td></tr></tbody></table></div></section>
     </div></t>
   </template>
@@ -27,7 +27,7 @@
       <section class="card"><div class="card-header"><strong>Current entries</strong></div><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Competition</th><th>Division</th><th>Team</th><th>Status</th></tr></thead><tbody><tr t-foreach="entries" t-as="entry"><td><t t-esc="entry.edition_id.display_name"/></td><td><t t-esc="entry.window_id.division_id.display_name"/></td><td><t t-esc="entry.team_id.display_name"/></td><td><span class="badge text-bg-light"><t t-esc="entry.state"/></span></td></tr></tbody></table></div></section>
     </div></t>
   </template>
-  <template id="portal_my_matchdays" name="My Match Days"><t t-call="portal.portal_layout"><div class="container py-3"><div class="d-flex justify-content-between"><div><h2>Match days</h2><p class="text-muted">Schedules are shown only from current live publications.</p></div><a href="/my/competitions" class="btn btn-outline-primary">Competitions</a></div><div class="list-group"><a t-foreach="matchdays" t-as="day" t-attf-href="/my/match-days/#{day.id}" class="list-group-item list-group-item-action"><strong><t t-esc="day.display_name"/></strong><span class="ms-2 badge text-bg-light">publication v<t t-esc="day.current_publication_id.version"/></span><br/><span class="text-muted"><t t-esc="day.date"/> · <t t-esc="day.venue_id.display_name"/></span></a></div><div class="alert alert-info mt-3" t-if="not matchdays">No published match days are visible.</div></div></t></template>
-  <template id="portal_my_matchday_detail" name="Match-Day Workspace"><t t-call="portal.portal_layout"><div class="container py-3"><a href="/my/match-days">← Match days</a><h2 class="mt-2"><t t-esc="workspace['matchday'].display_name"/></h2><p class="text-muted"><t t-esc="workspace['matchday'].date"/> · <t t-esc="workspace['matchday'].venue_id.display_name"/> · live publication v<t t-esc="workspace['publication'].version"/></p><div class="table-responsive"><table class="table"><thead><tr><th>Time</th><th>Match</th><th>Court</th><th>Operational status</th><th>Result</th></tr></thead><tbody><tr t-foreach="workspace['matches']" t-as="match"><td><t t-esc="match.date_scheduled"/></td><td><t t-esc="match.display_name"/></td><td><t t-esc="(match.operational_slot_id or match.published_slot_id).display_name"/></td><td><span class="badge text-bg-light"><t t-esc="match.operational_status"/></span></td><td><t t-esc="match.result_state"/></td></tr></tbody></table></div></div></t></template>
-  <template id="portal_matchday_operations_page" name="Match-Day Operations"><t t-call="portal.portal_layout"><div class="container-fluid py-3"><a href="/my/match-days">← Match days</a><div class="d-flex justify-content-between align-items-start mt-2"><div><h2><t t-esc="workspace['matchday'].display_name"/></h2><p class="text-muted">Current publication v<t t-esc="workspace['publication'].version"/> · <t t-esc="workspace['publication'].state"/></p></div><span class="badge text-bg-danger" t-if="workspace['session']">Live session</span></div><div class="row g-3"><div class="col-12 col-xl-8"><div class="card"><div class="card-header"><strong>Published matches</strong></div><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Match</th><th>Published</th><th>Operational</th><th>Status</th></tr></thead><tbody><tr t-foreach="workspace['matches']" t-as="match"><td><t t-esc="match.display_name"/></td><td><t t-esc="match.published_slot_id.display_name"/></td><td><t t-esc="match.operational_slot_id.display_name"/></td><td><t t-esc="match.operational_status"/></td></tr></tbody></table></div></div></div><div class="col-12 col-xl-4"><div class="card mb-3"><div class="card-header"><strong>Courts</strong></div><ul class="list-group list-group-flush"><li t-foreach="workspace['courts']" t-as="court" class="list-group-item"><t t-esc="court.court_id.display_name"/> · <t t-esc="court.state"/></li></ul></div><div class="card"><div class="card-header"><strong>Open incidents</strong></div><ul class="list-group list-group-flush"><li t-foreach="workspace['incidents'].filtered(lambda i: not i.resolved)" t-as="incident" class="list-group-item"><t t-esc="incident.description"/></li></ul></div></div></div></div></t></template>
+  <template id="portal_my_matchdays" name="My Match Days"><t t-call="portal.portal_layout"><div class="container py-3"><div class="d-flex justify-content-between"><div><h2>Match days</h2><p class="text-muted">Schedules are shown only from current live publications.</p></div><a href="/my/competitions" class="btn btn-outline-primary">Competitions</a></div><div class="list-group"><a t-foreach="matchdays" t-as="day" t-attf-href="/my/match-days/#{day.id}" class="list-group-item list-group-item-action"><strong><t t-esc="day.display_name"/></strong><span class="ms-2 badge text-bg-light">official schedule revision <t t-esc="day.current_publication_id.version"/></span><br/><span class="text-muted"><t t-esc="day.date"/> · <t t-esc="day.venue_id.display_name"/></span></a></div><div class="alert alert-info mt-3" t-if="not matchdays">No published match days are visible.</div></div></t></template>
+  <template id="portal_my_matchday_detail" name="Match-Day Workspace"><t t-call="portal.portal_layout"><div class="container py-3"><a href="/my/match-days">← Match days</a><h2 class="mt-2"><t t-esc="workspace['matchday'].display_name"/></h2><p class="text-muted"><t t-esc="workspace['matchday'].date"/> · <t t-esc="workspace['matchday'].venue_id.display_name"/> · live official schedule revision <t t-esc="workspace['publication'].version"/></p><div class="table-responsive"><table class="table"><thead><tr><th>Time</th><th>Match</th><th>Court</th><th>Operational status</th><th>Result</th></tr></thead><tbody><tr t-foreach="workspace['matches']" t-as="match"><td><t t-esc="match.date_scheduled"/></td><td><t t-esc="match.display_name"/></td><td><t t-esc="(match.operational_slot_id or match.published_slot_id).display_name"/></td><td><span class="badge text-bg-light"><t t-esc="match.operational_status"/></span></td><td><t t-esc="match.result_state"/></td></tr></tbody></table></div></div></t></template>
+  <template id="portal_matchday_operations_page" name="Match-Day Operations"><t t-call="portal.portal_layout"><div class="container-fluid py-3"><a href="/my/match-days">← Match days</a><div class="d-flex justify-content-between align-items-start mt-2"><div><h2><t t-esc="workspace['matchday'].display_name"/></h2><p class="text-muted">Current official schedule revision <t t-esc="workspace['publication'].version"/> · <t t-esc="workspace['publication'].state"/></p></div><span class="badge text-bg-danger" t-if="workspace['session']">Live session</span></div><div class="row g-3"><div class="col-12 col-xl-8"><div class="card"><div class="card-header"><strong>Published matches</strong></div><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Match</th><th>Published</th><th>Operational</th><th>Status</th></tr></thead><tbody><tr t-foreach="workspace['matches']" t-as="match"><td><t t-esc="match.display_name"/></td><td><t t-esc="match.published_slot_id.display_name"/></td><td><t t-esc="match.operational_slot_id.display_name"/></td><td><t t-esc="match.operational_status"/></td></tr></tbody></table></div></div></div><div class="col-12 col-xl-4"><div class="card mb-3"><div class="card-header"><strong>Courts</strong></div><ul class="list-group list-group-flush"><li t-foreach="workspace['courts']" t-as="court" class="list-group-item"><t t-esc="court.court_id.display_name"/> · <t t-esc="court.state"/></li></ul></div><div class="card"><div class="card-header"><strong>Open incidents</strong></div><ul class="list-group list-group-flush"><li t-foreach="workspace['incidents'].filtered(lambda i: not i.resolved)" t-as="incident" class="list-group-item"><t t-esc="incident.description"/></li></ul></div></div></div></div></t></template>
 </odoo>
diff --git a/sports_federation_public_site/__manifest__.py b/sports_federation_public_site/__manifest__.py
index b46af96..13b8941 100644
--- a/sports_federation_public_site/__manifest__.py
+++ b/sports_federation_public_site/__manifest__.py
@@ -25,7 +25,7 @@
     "data": [
         "security/ir.model.access.csv",
         "views/website_templates.xml",
-        "views/website_v2_competition_templates.xml",
+        "views/website_competition_templates.xml",
         "views/competition_edition_views_inherit.xml",
         "views/website_hub_templates.xml",
         "views/website_editorial_templates.xml",
@@ -39,7 +39,7 @@
     ],
     "assets": {
         "web.assets_frontend": [
-            "sports_federation_public_site/static/src/scss/public_v2_competitions.scss",
+            "sports_federation_public_site/static/src/scss/public_competitions_current.scss",
             "sports_federation_public_site/static/src/js/public_live_board.js",
         ],
     },
diff --git a/sports_federation_public_site/controllers/__init__.py b/sports_federation_public_site/controllers/__init__.py
index 40f6061..25eec52 100644
--- a/sports_federation_public_site/controllers/__init__.py
+++ b/sports_federation_public_site/controllers/__init__.py
@@ -1,4 +1,4 @@
 from . import public_competitions
 from . import public_follow
 from . import public_clubs_players
-from . import public_v2_competitions
+from . import public_competitions_current
diff --git a/sports_federation_public_site/controllers/public_v2_competitions.py b/sports_federation_public_site/controllers/public_competitions_current.py
similarity index 82%
rename from sports_federation_public_site/controllers/public_v2_competitions.py
rename to sports_federation_public_site/controllers/public_competitions_current.py
index 6eac3e5..9ac117d 100644
--- a/sports_federation_public_site/controllers/public_v2_competitions.py
+++ b/sports_federation_public_site/controllers/public_competitions_current.py
@@ -4,7 +4,7 @@ from odoo import http
 from odoo.http import Response, request
 
 
-class PublicV2CompetitionController(http.Controller):
+class PublicCompetitionController(http.Controller):
     def _queries(self):
         return request.env["federation.public.competition.queries"]
 
@@ -32,21 +32,11 @@ class PublicV2CompetitionController(http.Controller):
         sitemap=False,
     )
     def legacy_tournament_detail(self, tournament_slug=None, tournament_id=None, **kw):
-        domain = [
-            ("website_published", "=", True),
-            ("edition_id.website_published", "=", True),
-        ]
-        if tournament_id:
-            domain.append(("id", "=", tournament_id))
-        else:
-            domain.append(("public_slug", "=", tournament_slug))
-        division = request.env["federation.tournament"].sudo().search(domain, limit=1)
-        if not division or not division.edition_id.public_slug:
+        division = self._queries().resolve_legacy_division(slug=tournament_slug, division_id=tournament_id)
+        location = self._queries().canonical_location(division)
+        if not location:
             raise request.not_found()
-        return request.redirect(
-            f"/competitions/{division.edition_id.public_slug}?division_id={division.id}",
-            code=301,
-        )
+        return request.redirect(location, code=301)
 
     @http.route(
         ["/competitions", "/competitions/page/<int:page>"],
@@ -60,7 +50,7 @@ class PublicV2CompetitionController(http.Controller):
             archived=bool(archive), search=search, season_id=season_id
         )
         return request.render(
-            "sports_federation_public_site.public_v2_competition_hub",
+            "sports_federation_public_site.public_competition_competition_hub",
             {
                 "editions": editions,
                 "search": search,
@@ -91,11 +81,9 @@ class PublicV2CompetitionController(http.Controller):
     def competition_detail(self, edition_slug, division_id=None, **kw):
         edition = self._edition_or_404(edition_slug)
         summary = self._queries().edition_summary(edition)
-        division = (
-            summary["divisions"].filtered(lambda d: d.id == int(division_id))[:1]
-            if division_id
-            else summary["divisions"][:1]
-        )
+        division = self._queries().resolve_division(edition, division_id)
+        if division_id and not division:
+            raise request.not_found()
         summary.update(
             {
                 "division": division,
@@ -105,7 +93,7 @@ class PublicV2CompetitionController(http.Controller):
             }
         )
         return request.render(
-            "sports_federation_public_site.public_v2_competition_detail", summary
+            "sports_federation_public_site.public_competition_competition_detail", summary
         )
 
     @http.route(
@@ -118,13 +106,11 @@ class PublicV2CompetitionController(http.Controller):
     def competition_format(self, edition_slug, division_id=None, **kw):
         edition = self._edition_or_404(edition_slug)
         divisions = self._queries().public_divisions(edition)
-        division = (
-            divisions.filtered(lambda d: d.id == int(division_id))[:1]
-            if division_id
-            else divisions[:1]
-        )
+        division = self._queries().resolve_division(edition, division_id)
+        if division_id and not division:
+            raise request.not_found()
         return request.render(
-            "sports_federation_public_site.public_v2_competition_format",
+            "sports_federation_public_site.public_competition_competition_format",
             {
                 "edition": edition,
                 "divisions": divisions,
@@ -132,6 +118,7 @@ class PublicV2CompetitionController(http.Controller):
                 "stage_cards": request.env[
                     "federation.public.format.queries"
                 ].stage_cards(edition, division),
+                "page_name": "format",
             },
         )
 
@@ -148,8 +135,8 @@ class PublicV2CompetitionController(http.Controller):
             edition
         )
         return request.render(
-            "sports_federation_public_site.public_v2_competition_schedule",
-            {"edition": edition, "matchdays": matchdays},
+            "sports_federation_public_site.public_competition_competition_schedule",
+            {"edition": edition, "matchdays": matchdays, "page_name": "schedule"},
         )
 
     @http.route(
@@ -197,9 +184,9 @@ class PublicV2CompetitionController(http.Controller):
         )
         return request.render(
             (
-                "sports_federation_public_site.public_v2_gameday_display"
+                "sports_federation_public_site.public_competition_gameday_display"
                 if display
-                else "sports_federation_public_site.public_v2_gameday"
+                else "sports_federation_public_site.public_competition_gameday"
             ),
             board,
         )
diff --git a/sports_federation_public_site/migrations/19.0.2.0.0/post-migrate.py b/sports_federation_public_site/migrations/19.0.2.0.0/post-migrate.py
index b748900..20b492e 100644
--- a/sports_federation_public_site/migrations/19.0.2.0.0/post-migrate.py
+++ b/sports_federation_public_site/migrations/19.0.2.0.0/post-migrate.py
@@ -12,7 +12,7 @@ def migrate(cr, version):
            AND (website_published IS TRUE OR public_featured IS TRUE)
     """)
     _logger.info(
-        "V2 public cutover unpublished %s legacy tournament records", cr.rowcount
+        "current public cutover unpublished %s legacy tournament records", cr.rowcount
     )
     cr.execute("""
         UPDATE federation_tournament tournament
@@ -24,6 +24,6 @@ def migrate(cr, version):
            AND (tournament.website_published IS TRUE OR tournament.public_featured IS TRUE)
     """)
     _logger.info(
-        "V2 public cutover unpublished %s child divisions of private editions",
+        "current public cutover unpublished %s child divisions of private editions",
         cr.rowcount,
     )
diff --git a/sports_federation_public_site/services/public_competition_queries.py b/sports_federation_public_site/services/public_competition_queries.py
index 9c62917..a60fd95 100644
--- a/sports_federation_public_site/services/public_competition_queries.py
+++ b/sports_federation_public_site/services/public_competition_queries.py
@@ -58,6 +58,30 @@ class PublicCompetitionQueries(models.AbstractModel):
             and division.edition_id == edition
         ).sorted(lambda division: (division.name or "", division.id))
 
+    def resolve_division(self, edition, division_id=None):
+        divisions = self.public_divisions(edition)
+        if not division_id:
+            return divisions[:1]
+        try:
+            division_id = int(division_id)
+        except (TypeError, ValueError):
+            return self.env["federation.tournament"]
+        return divisions.filtered(lambda division: division.id == division_id)[:1]
+
+    def resolve_legacy_division(self, slug=None, division_id=None):
+        domain = [("active", "=", True), ("website_published", "=", True), ("edition_id.website_published", "=", True), ("edition_id.active", "=", True)]
+        domain.append(("id", "=", division_id) if division_id else ("public_slug", "=", slug))
+        division = self.env["federation.tournament"].sudo().search(domain, limit=1)
+        if not division or not self.resolve_edition(division.edition_id.public_slug):
+            return self.env["federation.tournament"]
+        return division
+
+    def canonical_location(self, division, section=None):
+        suffixes = {None: "", "teams": "", "standings": "", "results": "", "schedule": "/schedule", "bracket": "/format", "format": "/format"}
+        if not division or section not in suffixes:
+            return False
+        return "/competitions/%s%s?division_id=%s" % (division.edition_id.public_slug, suffixes[section], division.id)
+
     def edition_summary(self, edition):
         divisions = self.public_divisions(edition)
         matchdays = (
diff --git a/sports_federation_public_site/static/src/scss/public_v2_competitions.scss b/sports_federation_public_site/static/src/scss/public_competitions.scss
similarity index 70%
rename from sports_federation_public_site/static/src/scss/public_v2_competitions.scss
rename to sports_federation_public_site/static/src/scss/public_competitions.scss
index 07a005c..fe46971 100644
--- a/sports_federation_public_site/static/src/scss/public_v2_competitions.scss
+++ b/sports_federation_public_site/static/src/scss/public_competitions.scss
@@ -17,3 +17,10 @@
 .sf-display-board .sf-match-card { font-size: 1.2rem; min-height: 8rem; }
 @media (max-width: 767.98px) { .sf-standings-table { font-size: .85rem; } .sf-stage { padding-top: 1rem; } }
 @media print { header, nav, form, footer, .btn { display: none !important; } .sf-gameday-grid { min-width: 0; } }
+
+.sf-competition-card { transition: transform 160ms ease, box-shadow 160ms ease; }
+.sf-competition-card:hover, .sf-competition-card:focus-within { transform: translateY(-3px); box-shadow: 0 1rem 2rem rgba(21,38,65,.12) !important; }
+.sf-competition-nav { gap: .35rem; padding-bottom: .25rem; scrollbar-width: thin; }
+.sf-competition-nav .nav-link, .sf-live-board .form-select, .sf-live-board .btn, .sf-competition-card .btn { min-height: 44px; display: inline-flex; align-items: center; }
+.sf-empty-state { max-width: 48rem; margin-inline: auto; padding: clamp(2rem,6vw,4rem); text-align:center; border:1px dashed var(--bs-border-color); border-radius:1rem; background:var(--bs-tertiary-bg); }
+@media (prefers-reduced-motion: reduce) { .sf-competition-card { transition:none; } }
diff --git a/sports_federation_public_site/tests/__init__.py b/sports_federation_public_site/tests/__init__.py
index 03149ae..fa62158 100644
--- a/sports_federation_public_site/tests/__init__.py
+++ b/sports_federation_public_site/tests/__init__.py
@@ -5,4 +5,4 @@ from . import test_template_accessibility
 from . import test_tour_publication
 from . import test_club_team_pages
 from . import test_player_pages
-from . import test_public_v2_contract
+from . import test_public_competition_contract
diff --git a/sports_federation_public_site/tests/test_club_team_pages.py b/sports_federation_public_site/tests/test_club_team_pages.py
index 5ce2242..7b96cf4 100644
--- a/sports_federation_public_site/tests/test_club_team_pages.py
+++ b/sports_federation_public_site/tests/test_club_team_pages.py
@@ -1,5 +1,5 @@
 """
-Smoke tests for Phase 2: public club and team profile pages.
+Smoke tests for Rules: public club and team profile pages.
 
 Asserts:
   - /clubs returns HTTP 200
diff --git a/sports_federation_public_site/tests/test_player_pages.py b/sports_federation_public_site/tests/test_player_pages.py
index 2f4ad87..9866089 100644
--- a/sports_federation_public_site/tests/test_player_pages.py
+++ b/sports_federation_public_site/tests/test_player_pages.py
@@ -1,5 +1,5 @@
 """
-Smoke tests for Phase 2: public player profile pages.
+Smoke tests for Rules: public player profile pages.
 
 Asserts:
   - /players returns HTTP 200
diff --git a/sports_federation_public_site/tests/test_public_api.py b/sports_federation_public_site/tests/test_public_api.py
index c45a3e0..748c407 100644
--- a/sports_federation_public_site/tests/test_public_api.py
+++ b/sports_federation_public_site/tests/test_public_api.py
@@ -1,5 +1,5 @@
 """
-Tests for new public site endpoints (Phase 4):
+Tests for new public site endpoints (API):
 - /competitions/archive — closed/cancelled tournaments
 - /competitions/<id>/teams — participant listing
 - /competitions/api/json — JSON API tournament list
diff --git a/sports_federation_public_site/tests/test_public_v2_contract.py b/sports_federation_public_site/tests/test_public_competition_contract.py
similarity index 96%
rename from sports_federation_public_site/tests/test_public_v2_contract.py
rename to sports_federation_public_site/tests/test_public_competition_contract.py
index 0f2a8cd..b61474c 100644
--- a/sports_federation_public_site/tests/test_public_v2_contract.py
+++ b/sports_federation_public_site/tests/test_public_competition_contract.py
@@ -1,7 +1,7 @@
 from odoo.tests.common import TransactionCase
 
 
-class TestPublicV2Contract(TransactionCase):
+class TestPublicCompetitionContract(TransactionCase):
     def test_legacy_division_is_not_public(self):
         legacy = self.env["federation.tournament"].create(
             {
diff --git a/sports_federation_public_site/views/competition_edition_views_inherit.xml b/sports_federation_public_site/views/competition_edition_views_inherit.xml
index 93b50af..19d80c1 100644
--- a/sports_federation_public_site/views/competition_edition_views_inherit.xml
+++ b/sports_federation_public_site/views/competition_edition_views_inherit.xml
@@ -1,6 +1,6 @@
 <odoo>
-  <record id="view_competition_edition_form_public_v2" model="ir.ui.view">
-    <field name="name">federation.competition.edition.form.public.v2</field>
+  <record id="view_competition_edition_form_public_competition" model="ir.ui.view">
+    <field name="name">federation.competition.edition.form.public.current</field>
     <field name="model">federation.competition.edition</field>
     <field name="inherit_id" ref="sports_federation_tournament.view_federation_competition_edition_form"/>
     <field name="arch" type="xml">
diff --git a/sports_federation_public_site/views/website_v2_competition_templates.xml b/sports_federation_public_site/views/website_competition_templates.xml
similarity index 74%
rename from sports_federation_public_site/views/website_v2_competition_templates.xml
rename to sports_federation_public_site/views/website_competition_templates.xml
index ee15027..19caa35 100644
--- a/sports_federation_public_site/views/website_v2_competition_templates.xml
+++ b/sports_federation_public_site/views/website_competition_templates.xml
@@ -1,5 +1,5 @@
 <odoo>
-  <template id="public_v2_competition_card" name="Competition card">
+  <template id="public_competition_competition_card" name="Competition card">
     <article class="sf-competition-card card h-100 shadow-sm border-0">
       <div class="card-body d-flex flex-column">
         <div class="d-flex justify-content-between gap-2 mb-2">
@@ -16,7 +16,7 @@
     </article>
   </template>
 
-  <template id="public_v2_match_card" name="Public match card">
+  <template id="public_competition_match_card" name="Public match card">
     <article class="sf-match-card" t-att-data-match-id="row['match'].id">
       <div class="d-flex justify-content-between gap-2 small text-muted mb-1">
         <span t-if="row['slot']"><t t-esc="row['slot'].court_id.display_name"/></span>
@@ -28,55 +28,55 @@
     </article>
   </template>
 
-  <template id="public_v2_competition_hub" name="Competitions">
+  <template id="public_competition_competition_hub" name="Competitions">
     <t t-call="website.layout"><main class="container py-5">
       <header class="d-flex flex-column flex-lg-row justify-content-between align-items-lg-end gap-3 mb-4">
-        <div><p class="text-primary text-uppercase fw-semibold mb-1">Competition centre</p><h1><t t-esc="'Competition archive' if archive else 'Current competitions'"/></h1><p class="lead text-muted">Schedules, gamedays, standings and knockout brackets from the published competition schedule.</p></div>
+        <div><p class="text-primary text-uppercase fw-semibold mb-1">Competition centre</p><h1><t t-esc="'Competition archive' if archive else 'Current competitions'"/></h1><p class="lead text-muted">Follow schedules, live match days, results, standings and brackets.</p></div>
         <a class="btn btn-outline-secondary" t-att-href="'/competitions' if archive else '/competitions/archive'"><t t-esc="'Current competitions' if archive else 'Archive'"/></a>
       </header>
       <form class="row g-2 mb-4" method="get">
-        <div class="col-md-8"><label class="visually-hidden" for="competition-search">Search</label><input id="competition-search" class="form-control" name="search" t-att-value="search" placeholder="Search competitions"/></div>
+        <div class="col-md-8"><label class="visually-hidden" for="competition-search">Search</label><input id="competition-search" class="form-control" name="search" t-att-value="search" placeholder="Search by competition name" autocomplete="off"/></div>
         <div class="col-md-3"><select class="form-select" name="season_id" aria-label="Season"><option value="">All seasons</option><option t-foreach="seasons" t-as="season" t-att-value="season.id"><t t-esc="season.display_name"/></option></select></div>
-        <div class="col-md-1"><button class="btn btn-primary w-100">Go</button></div>
+        <div class="col-md-1"><button class="btn btn-primary w-100">Search</button></div>
       </form>
-      <div class="row g-4" t-if="editions"><div class="col-md-6 col-xl-4" t-foreach="editions" t-as="edition"><t t-call="sports_federation_public_site.public_v2_competition_card"/></div></div>
+      <div class="row g-4" t-if="editions"><div class="col-md-6 col-xl-4" t-foreach="editions" t-as="edition"><t t-call="sports_federation_public_site.public_competition_competition_card"/></div></div>
       <div class="sf-empty-state" t-else=""><h2 class="h4">No competitions found</h2><p class="text-muted">Published competitions will appear here.</p></div>
     </main></t>
   </template>
 
-  <template id="public_v2_competition_nav" name="Competition navigation">
-    <nav class="nav nav-pills flex-nowrap overflow-auto mb-4" aria-label="Competition sections">
-      <a class="nav-link" t-att-href="'/competitions/%s' % edition.public_slug">Overview</a>
-      <a class="nav-link" t-att-href="'/competitions/%s/schedule' % edition.public_slug">Schedule</a>
-      <a class="nav-link" t-att-href="'/competitions/%s/format' % edition.public_slug">Format</a>
+  <template id="public_competition_competition_nav" name="Competition navigation">
+    <nav class="nav nav-pills flex-nowrap overflow-auto mb-4 sf-competition-nav" aria-label="Competition sections">
+      <a t-att-class="'nav-link ' + ('active' if page_name == 'overview' else '')" t-att-aria-current="'page' if page_name == 'overview' else None" t-att-href="'/competitions/%s' % edition.public_slug">Overview</a>
+      <a t-att-class="'nav-link ' + ('active' if page_name == 'schedule' else '')" t-att-aria-current="'page' if page_name == 'schedule' else None" t-att-href="'/competitions/%s/schedule' % edition.public_slug">Schedule</a>
+      <a t-att-class="'nav-link ' + ('active' if page_name == 'format' else '')" t-att-aria-current="'page' if page_name == 'format' else None" t-att-href="'/competitions/%s/format' % edition.public_slug">Format and standings</a>
     </nav>
   </template>
 
-  <template id="public_v2_competition_detail" name="Competition detail">
+  <template id="public_competition_competition_detail" name="Competition detail">
     <t t-call="website.layout"><main>
       <section class="sf-competition-hero"><div class="container py-5"><span class="badge text-bg-light mb-2"><t t-esc="edition.engine_state"/></span><h1><t t-esc="edition.name"/></h1><p class="lead mb-0"><t t-esc="edition.public_summary or edition.season_id.display_name"/></p></div></section>
-      <div class="container py-4"><t t-call="sports_federation_public_site.public_v2_competition_nav"/>
+      <div class="container py-4"><t t-call="sports_federation_public_site.public_competition_competition_nav"/>
         <div class="d-flex flex-wrap gap-2 mb-4"><a t-foreach="divisions" t-as="item" t-att-class="'btn ' + ('btn-primary' if division == item else 'btn-outline-primary')" t-att-href="'/competitions/%s?division_id=%s' % (edition.public_slug, item.id)"><t t-esc="item.name"/></a></div>
         <div class="row g-4">
-          <div class="col-lg-7"><section class="card border-0 shadow-sm"><div class="card-body"><h2 class="h4">Next gameday</h2><t t-if="next_matchday"><h3 class="h5"><t t-esc="next_matchday.name"/></h3><p><t t-esc="next_matchday.date"/> · <t t-esc="next_matchday.venue_id.display_name"/></p><a class="btn btn-primary" t-att-href="'/competitions/%s/gamedays/%s' % (edition.public_slug, next_matchday.id)">Open gameday board</a></t><p class="text-muted" t-else="">No published gameday is currently available.</p></div></section></div>
+          <div class="col-lg-7"><section class="card border-0 shadow-sm"><div class="card-body"><h2 class="h4">Next match day</h2><t t-if="next_matchday"><h3 class="h5"><t t-esc="next_matchday.name"/></h3><p><t t-esc="next_matchday.date"/> · <t t-esc="next_matchday.venue_id.display_name"/></p><a class="btn btn-primary" t-att-href="'/competitions/%s/gamedays/%s' % (edition.public_slug, next_matchday.id)">Open match-day schedule</a></t><p class="text-muted" t-else="">No published match day is currently available.</p></div></section></div>
           <div class="col-lg-5"><section class="card border-0 shadow-sm"><div class="card-body"><h2 class="h4">Current format</h2><t t-if="current_stage"><p class="mb-1"><strong><t t-esc="current_stage.name"/></strong></p><span class="badge text-bg-info"><t t-esc="current_stage.format_type.replace('_', ' ').title()"/></span><p class="text-muted mt-2"><t t-esc="current_stage.graph_state.title()"/></p><a t-att-href="'/competitions/%s/format?division_id=%s' % (edition.public_slug, division.id if division else '')">View full competition format</a></t><p class="text-muted" t-else="">The public format will appear after the structure is frozen.</p></div></section></div>
         </div>
       </div>
     </main></t>
   </template>
 
-  <template id="public_v2_competition_schedule" name="Competition schedule">
-    <t t-call="website.layout"><main class="container py-5"><h1><t t-esc="edition.name"/> schedule</h1><t t-call="sports_federation_public_site.public_v2_competition_nav"/>
-      <div class="row g-3" t-if="matchdays"><div class="col-md-6" t-foreach="matchdays" t-as="matchday"><article class="card h-100 shadow-sm"><div class="card-body"><p class="text-primary fw-semibold"><t t-esc="matchday.date"/></p><h2 class="h5"><t t-esc="matchday.name"/></h2><p class="text-muted"><t t-esc="matchday.venue_id.display_name"/> · Publication v<t t-esc="matchday.current_publication_id.version"/></p><a class="btn btn-outline-primary" t-att-href="'/competitions/%s/gamedays/%s' % (edition.public_slug, matchday.id)">View gameday</a></div></article></div></div>
+  <template id="public_competition_competition_schedule" name="Competition schedule">
+    <t t-call="website.layout"><main class="container py-5"><h1><t t-esc="edition.name"/> schedule</h1><t t-call="sports_federation_public_site.public_competition_competition_nav"/>
+      <div class="row g-3" t-if="matchdays"><div class="col-md-6" t-foreach="matchdays" t-as="matchday"><article class="card h-100 shadow-sm"><div class="card-body"><p class="text-primary fw-semibold"><t t-esc="matchday.date"/></p><h2 class="h5"><t t-esc="matchday.name"/></h2><p class="text-muted"><t t-esc="matchday.venue_id.display_name"/> · Publication v<t t-esc="matchday.current_publication_id.version"/></p><a class="btn btn-outline-primary" t-att-href="'/competitions/%s/gamedays/%s' % (edition.public_slug, matchday.id)">View match day</a></div></article></div></div>
       <div class="sf-empty-state" t-else=""><h2 class="h4">Schedule not published</h2><p>Only current live schedule publications are shown.</p></div>
     </main></t>
   </template>
 
-  <template id="public_v2_stage_cards" name="V2 stage cards">
+  <template id="public_competition_stage_cards" name="current stage cards">
     <section class="sf-stage" t-foreach="stage_cards" t-as="card">
       <header class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3"><div><p class="text-uppercase text-primary small fw-semibold mb-1">Stage <t t-esc="card['stage_index'] if 'stage_index' in card else ''"/></p><h2 class="h3 mb-0"><t t-esc="card['stage'].name"/></h2></div><div><span class="badge text-bg-primary"><t t-esc="card['stage'].format_type.replace('_', ' ').title()"/></span> <span class="badge text-bg-light"><t t-esc="card['stage'].graph_state.title()"/></span></div></header>
       <p class="text-muted" t-if="card['progressions']"><t t-foreach="card['progressions']" t-as="progression"><span>Ranks <t t-esc="progression.rank_from"/>–<t t-esc="progression.rank_to"/> progress to <strong><t t-esc="progression.target_stage_id.name"/></strong>. </span></t></p>
-      <div class="table-responsive mb-4" t-if="card['standings']"><table class="table table-hover align-middle sf-standings-table"><caption>Frozen standings for <t t-esc="card['stage'].name"/></caption><thead><tr><th>Pos</th><th>Team</th><th>P</th><th>W</th><th>D</th><th>L</th><th>For</th><th>Against</th><th>Diff</th><th>Pts</th></tr></thead><tbody><tr t-foreach="card['standings']" t-as="line"><td><strong><t t-esc="line.rank"/></strong></td><td><t t-esc="line.team_id.display_name"/></td><td><t t-esc="line.played"/></td><td><t t-esc="line.won"/></td><td><t t-esc="line.drawn"/></td><td><t t-esc="line.lost"/></td><td><t t-esc="line.score_for"/></td><td><t t-esc="line.score_against"/></td><td><t t-esc="line.score_for - line.score_against"/></td><td><strong><t t-esc="line.points"/></strong></td></tr></tbody></table></div>
+      <div class="table-responsive mb-4" t-if="card['standings']"><table class="table table-hover align-middle sf-standings-table"><caption>Frozen standings for <t t-esc="card['stage'].name"/></caption><thead><tr><th>Position</th><th>Team</th><th><abbr title="Played">P</abbr></th><th><abbr title="Won">W</abbr></th><th><abbr title="Drawn">D</abbr></th><th><abbr title="Lost">L</abbr></th><th>For</th><th>Against</th><th>Diff</th><th>Pts</th></tr></thead><tbody><tr t-foreach="card['standings']" t-as="line"><td><strong><t t-esc="line.rank"/></strong></td><td><t t-esc="line.team_id.display_name"/></td><td><t t-esc="line.played"/></td><td><t t-esc="line.won"/></td><td><t t-esc="line.drawn"/></td><td><t t-esc="line.lost"/></td><td><t t-esc="line.score_for"/></td><td><t t-esc="line.score_against"/></td><td><t t-esc="line.score_for - line.score_against"/></td><td><strong><t t-esc="line.points"/></strong></td></tr></tbody></table></div>
       <div class="sf-bracket" t-if="card['rounds'] and card['stage'].format_type in ('knockout','placement_bracket')" role="region" t-att-aria-label="card['stage'].name + ' bracket'">
         <section class="sf-bracket-round" t-foreach="card['rounds']" t-as="round_data"><h3 class="h6 text-center">Round <t t-esc="round_data[0]"/></h3><article class="sf-bracket-match" t-foreach="round_data[1]" t-as="fixture"><div class="sf-team-line"><span><t t-esc="fixture.home_team_id.display_name or 'TBD'"/></span><strong t-if="fixture.result_state in ('submitted','verified','approved')"><t t-esc="fixture.home_score"/></strong></div><div class="sf-team-line"><span><t t-esc="fixture.away_team_id.display_name or 'TBD'"/></span><strong t-if="fixture.result_state in ('submitted','verified','approved')"><t t-esc="fixture.away_score"/></strong></div><small class="text-muted" t-if="fixture.home_source_fixture_id or fixture.away_source_fixture_id">Progression fixture</small></article></section>
       </div>
@@ -84,21 +84,21 @@
     </section>
   </template>
 
-  <template id="public_v2_competition_format" name="Competition format">
-    <t t-call="website.layout"><main class="container py-5"><h1><t t-esc="edition.name"/> format</h1><t t-call="sports_federation_public_site.public_v2_competition_nav"/><div class="d-flex flex-wrap gap-2 mb-4"><a t-foreach="divisions" t-as="item" t-att-class="'btn ' + ('btn-primary' if division == item else 'btn-outline-primary')" t-att-href="'/competitions/%s/format?division_id=%s' % (edition.public_slug, item.id)"><t t-esc="item.name"/></a></div><t t-call="sports_federation_public_site.public_v2_stage_cards"/><div class="sf-empty-state" t-if="not stage_cards"><h2 class="h4">Format not published</h2><p>The published competition structure will appear here.</p></div></main></t>
+  <template id="public_competition_competition_format" name="Competition format">
+    <t t-call="website.layout"><main class="container py-5"><h1><t t-esc="edition.name"/> format</h1><t t-call="sports_federation_public_site.public_competition_competition_nav"/><div class="d-flex flex-wrap gap-2 mb-4"><a t-foreach="divisions" t-as="item" t-att-class="'btn ' + ('btn-primary' if division == item else 'btn-outline-primary')" t-att-href="'/competitions/%s/format?division_id=%s' % (edition.public_slug, item.id)"><t t-esc="item.name"/></a></div><t t-call="sports_federation_public_site.public_competition_stage_cards"/><div class="sf-empty-state" t-if="not stage_cards"><h2 class="h4">Format not published</h2><p>The published competition structure will appear here.</p></div></main></t>
   </template>
 
-  <template id="public_v2_gameday" name="V2 gameday board">
+  <template id="public_competition_gameday" name="current gameday board">
     <t t-call="website.layout"><main class="container-fluid px-lg-5 py-4 sf-live-board" t-att-data-status-url="'/competitions/%s/gamedays/%s/status.json' % (edition.public_slug, matchday.id)">
       <header class="d-flex flex-column flex-lg-row justify-content-between gap-3 mb-4"><div><a t-att-href="'/competitions/%s/schedule' % edition.public_slug">← Schedule</a><h1 class="mt-2"><t t-esc="matchday.name"/></h1><p class="text-muted"><t t-esc="matchday.date"/> · <t t-esc="matchday.venue_id.display_name"/> · Live publication v<t t-esc="publication.version"/></p></div><div class="d-flex gap-2 align-items-start"><button type="button" class="btn btn-outline-primary sf-jump-now">Jump to now</button><a class="btn btn-outline-secondary" t-att-href="'/competitions/%s/gamedays/%s?display=1' % (edition.public_slug, matchday.id)">Display mode</a></div></header>
       <form class="row g-2 mb-3"><div class="col-md-5"><select name="team_id" class="form-select" aria-label="Filter by team"><option value="">All teams</option><option t-foreach="edition.tournament_ids.mapped('participant_ids').mapped('team_id')" t-as="team" t-att-value="team.id" t-att-selected="team_id == team.id"><t t-esc="team.display_name"/></option></select></div><div class="col-md-5"><select name="court_id" class="form-select" aria-label="Filter by court"><option value="">All courts</option><option t-foreach="courts" t-as="court" t-att-value="court.id" t-att-selected="court_id == court.id"><t t-esc="court.display_name"/></option></select></div><div class="col-md-2"><button class="btn btn-primary w-100">Filter</button></div></form>
-      <div class="d-none d-lg-block table-responsive sf-gameday-grid-wrap" t-if="times and courts"><table class="table sf-gameday-grid"><caption class="visually-hidden">Matches by time and court</caption><thead><tr><th class="sf-time-column">Time</th><th t-foreach="courts" t-as="court"><t t-esc="court.display_name"/></th></tr></thead><tbody><tr t-foreach="times" t-as="time"><th class="sf-time-column" scope="row"><t t-esc="time.strftime('%H:%M')"/></th><td t-foreach="courts" t-as="court"><t t-set="row" t-value="grid.get((time, court.id))"/><t t-if="row" t-call="sports_federation_public_site.public_v2_match_card"/></td></tr></tbody></table></div>
-      <div class="d-lg-none sf-mobile-agenda" t-if="by_time"><section class="mb-4" t-foreach="by_time" t-as="time_rows"><h2 class="h5 sticky-top bg-white py-2"><t t-esc="time_rows[0].strftime('%H:%M') if time_rows[0] else 'Unscheduled'"/></h2><div class="mb-2" t-foreach="time_rows[1]" t-as="row"><t t-call="sports_federation_public_site.public_v2_match_card"/></div></section></div>
+      <div class="d-none d-lg-block table-responsive sf-gameday-grid-wrap" t-if="times and courts"><table class="table sf-gameday-grid"><caption class="visually-hidden">Matches by time and court</caption><thead><tr><th class="sf-time-column">Time</th><th t-foreach="courts" t-as="court"><t t-esc="court.display_name"/></th></tr></thead><tbody><tr t-foreach="times" t-as="time"><th class="sf-time-column" scope="row"><t t-esc="time.strftime('%H:%M')"/></th><td t-foreach="courts" t-as="court"><t t-set="row" t-value="grid.get((time, court.id))"/><t t-if="row" t-call="sports_federation_public_site.public_competition_match_card"/></td></tr></tbody></table></div>
+      <div class="d-lg-none sf-mobile-agenda" t-if="by_time"><section class="mb-4" t-foreach="by_time" t-as="time_rows"><h2 class="h5 sticky-top bg-white py-2"><t t-esc="time_rows[0].strftime('%H:%M') if time_rows[0] else 'Unscheduled'"/></h2><div class="mb-2" t-foreach="time_rows[1]" t-as="row"><t t-call="sports_federation_public_site.public_competition_match_card"/></div></section></div>
       <div class="sf-empty-state" t-if="not matches"><h2 class="h4">No published matches</h2><p>Change the filters or return after the schedule is published.</p></div>
     </main></t>
   </template>
 
-  <template id="public_v2_gameday_display" name="V2 gameday display">
-    <t t-call="website.layout"><main class="container-fluid p-4 sf-display-board sf-live-board" t-att-data-status-url="'/competitions/%s/gamedays/%s/status.json' % (edition.public_slug, matchday.id)"><header class="d-flex justify-content-between align-items-center mb-4"><div><h1><t t-esc="matchday.name"/></h1><p class="lead"><t t-esc="matchday.venue_id.display_name"/></p></div><div class="display-6" id="sf-display-clock"></div></header><div class="row g-3"><div class="col-md-6 col-xl-4" t-foreach="matches" t-as="row"><t t-call="sports_federation_public_site.public_v2_match_card"/></div></div></main></t>
+  <template id="public_competition_gameday_display" name="current gameday display">
+    <t t-call="website.layout"><main class="container-fluid p-4 sf-display-board sf-live-board" t-att-data-status-url="'/competitions/%s/gamedays/%s/status.json' % (edition.public_slug, matchday.id)"><header class="d-flex justify-content-between align-items-center mb-4"><div><h1><t t-esc="matchday.name"/></h1><p class="lead"><t t-esc="matchday.venue_id.display_name"/></p></div><div class="display-6" id="sf-display-clock"></div></header><div class="row g-3"><div class="col-md-6 col-xl-4" t-foreach="matches" t-as="row"><t t-call="sports_federation_public_site.public_competition_match_card"/></div></div></main></t>
   </template>
 </odoo>
diff --git a/sports_federation_registration/views/menu.xml b/sports_federation_registration/views/menu.xml
index e34a6af..20fe8f0 100644
--- a/sports_federation_registration/views/menu.xml
+++ b/sports_federation_registration/views/menu.xml
@@ -1,4 +1,4 @@
 <odoo>
- <menuitem id="menu_create_competition" name="Create Competition" parent="sports_federation_competition_core.menu_competition_engine_v2_root" action="action_create_competition_wizard" sequence="5"/>
- <menuitem id="menu_registration_desk" name="Registration Desk" parent="sports_federation_competition_core.menu_competition_engine_v2_root" action="action_registration_desk" sequence="20"/>
+ <menuitem id="menu_create_competition" name="Create Competition" parent="sports_federation_competition_core.menu_competition_engine_competition_root" action="action_create_competition_wizard" sequence="5"/>
+ <menuitem id="menu_registration_desk" name="Registration Desk" parent="sports_federation_competition_core.menu_competition_engine_competition_root" action="action_registration_desk" sequence="20"/>
 </odoo>
diff --git a/sports_federation_reporting/tests/test_kpi_csv.py b/sports_federation_reporting/tests/test_kpi_csv.py
index d1cad66..494f612 100644
--- a/sports_federation_reporting/tests/test_kpi_csv.py
+++ b/sports_federation_reporting/tests/test_kpi_csv.py
@@ -1,5 +1,5 @@
 """
-Tests for KPI CSV export controller (Phase 4).
+Tests for KPI CSV export controller (API).
 
 These are ORM-level tests that verify the data layer (queries + serialization)
 used by the CSV export endpoints without making real HTTP requests.
diff --git a/sports_federation_result_control/models/match_result_control.py b/sports_federation_result_control/models/match_result_control.py
index 209acd6..2abd1a7 100644
--- a/sports_federation_result_control/models/match_result_control.py
+++ b/sports_federation_result_control/models/match_result_control.py
@@ -171,12 +171,18 @@ class FederationMatchResultControl(models.Model):
             if Dispatcher is not None:
                 Dispatcher.send_result_submitted(rec)
 
+    def _lock_result_transition(self):
+        if self.ids:
+            self.env.cr.execute("SELECT id FROM federation_match WHERE id = ANY(%s) FOR UPDATE", (self.ids,))
+            self.invalidate_recordset(["result_state", "result_submitted_by_id", "result_verified_by_id", "result_contest_reason"])
+
     def action_verify_result(self):
         """Verify the submitted result."""
         self._check_result_group(
             "sports_federation_result_control.group_result_validator",
             "Only result validators can verify submitted results.",
         )
+        self._lock_result_transition()
         for rec in self:
             if rec.result_state != "submitted":
                 raise ValidationError("Only submitted results can be verified.")
@@ -205,6 +211,7 @@ class FederationMatchResultControl(models.Model):
             "sports_federation_result_control.group_result_approver",
             "Only result approvers can approve verified results.",
         )
+        self._lock_result_transition()
         for rec in self:
             if rec.result_state != "verified":
                 raise ValidationError("Only verified results can be approved.")
@@ -238,6 +245,7 @@ class FederationMatchResultControl(models.Model):
 
     def action_contest_result(self):
         """Contest a result (from submitted, verified, or approved)."""
+        self._lock_result_transition()
         for rec in self:
             if rec.result_state not in ("submitted", "verified", "approved"):
                 raise ValidationError(
diff --git a/sports_federation_rosters/tests/test_roster_scope_uniqueness.py b/sports_federation_rosters/tests/test_roster_scope_uniqueness.py
index edf8885..7ed8042 100644
--- a/sports_federation_rosters/tests/test_roster_scope_uniqueness.py
+++ b/sports_federation_rosters/tests/test_roster_scope_uniqueness.py
@@ -1,4 +1,4 @@
-"""Regression tests for roster scope uniqueness (Phase 2, Items 3 & 4).
+"""Regression tests for roster scope uniqueness (Rules, Items 3 & 4).
 
 Verifies the correct semantics after removing the name-based unique constraint:
 
diff --git a/sports_federation_rules/tests/test_eligibility_service.py b/sports_federation_rules/tests/test_eligibility_service.py
index 73f45d1..39a8876 100644
--- a/sports_federation_rules/tests/test_eligibility_service.py
+++ b/sports_federation_rules/tests/test_eligibility_service.py
@@ -1,5 +1,5 @@
 """
-Tests for federation.eligibility.service (Phase 3).
+Tests for federation.eligibility.service (Integration).
 
 Coverage:
 - age_min rule: under-age player rejected, old-enough player passes
diff --git a/sports_federation_rules/tests/test_standings_rules.py b/sports_federation_rules/tests/test_standings_rules.py
index 78c2f01..d09677d 100644
--- a/sports_federation_rules/tests/test_standings_rules.py
+++ b/sports_federation_rules/tests/test_standings_rules.py
@@ -2,14 +2,14 @@ from odoo.tests import tagged
 from odoo.tests.common import TransactionCase
 
 
-@tagged("post_install", "-at_install", "sf_phase2_rules")
+@tagged("post_install", "-at_install", "sf_legacy_rules")
 class TestStandingsRules(TransactionCase):
     def setUp(self):
         super().setUp()
         self.engine = self.env["federation.standings.rules"]
         self.rule_set = self.env["federation.rule.set"].create(
             {
-                "name": "Phase 2",
+                "name": "Rules",
                 "points_win": 5,
                 "points_draw": 2,
                 "points_loss": -1,
diff --git a/sports_federation_schedule_approval/README.md b/sports_federation_schedule_approval/README.md
index 9a8afba..f72ecb0 100644
--- a/sports_federation_schedule_approval/README.md
+++ b/sports_federation_schedule_approval/README.md
@@ -33,8 +33,8 @@ pending; state and audit fields remain command-service-only.
 ## Tests and contracts
 
 ```bash
-python ci/check_phases_3_5_contract.py
-python ci/check_phase51_schedule_handoff.py
+python ci/check_competition_pipeline_contract.py
+python ci/check_schedule_handoff_contract.py
 bash ci/run_tests.sh --module sports_federation_schedule_approval
 bash ci/run_tests.sh --suite competition_core
 ```
diff --git a/sports_federation_schedule_approval/tests/__init__.py b/sports_federation_schedule_approval/tests/__init__.py
index 7c30b9b..aa79fd4 100644
--- a/sports_federation_schedule_approval/tests/__init__.py
+++ b/sports_federation_schedule_approval/tests/__init__.py
@@ -1,2 +1,2 @@
-from . import test_phase51_handoff
-from . import test_phase511_integrity
+from . import test_schedule_handoff
+from . import test_publication_integrity
diff --git a/sports_federation_schedule_approval/tests/test_phase511_integrity.py b/sports_federation_schedule_approval/tests/test_publication_integrity.py
similarity index 95%
rename from sports_federation_schedule_approval/tests/test_phase511_integrity.py
rename to sports_federation_schedule_approval/tests/test_publication_integrity.py
index 3a1738b..21230b4 100644
--- a/sports_federation_schedule_approval/tests/test_phase511_integrity.py
+++ b/sports_federation_schedule_approval/tests/test_publication_integrity.py
@@ -6,7 +6,7 @@ from odoo.tests import tagged
 from odoo.tests.common import TransactionCase
 
 
-@tagged("post_install", "-at_install", "sf_phase511")
+@tagged("post_install", "-at_install", "sf_publication")
 class TestPhase511Integrity(TransactionCase):
     @classmethod
     def setUpClass(cls):
@@ -256,9 +256,9 @@ class TestPhase511Integrity(TransactionCase):
             .with_context(no_reset_password=True)
             .create(
                 {
-                    "name": "Phase 5.1.1 Schedule Approver",
-                    "login": "phase511.schedule.approver@example.com",
-                    "email": "phase511.schedule.approver@example.com",
+                    "name": "Publication integrity Schedule Approver",
+                    "login": "publication.schedule.approver@example.com",
+                    "email": "publication.schedule.approver@example.com",
                     "group_ids": [(6, 0, [approver_group.id])],
                 }
             )
@@ -299,9 +299,9 @@ class TestPhase511Integrity(TransactionCase):
             .with_context(no_reset_password=True)
             .create(
                 {
-                    "name": "Phase 5.1.1 Unassigned Approver",
-                    "login": "phase511.unassigned.approver@example.com",
-                    "email": "phase511.unassigned.approver@example.com",
+                    "name": "Publication integrity Unassigned Approver",
+                    "login": "publication.unassigned.approver@example.com",
+                    "email": "publication.unassigned.approver@example.com",
                     "group_ids": [(6, 0, [approver_group.id])],
                 }
             )
diff --git a/sports_federation_schedule_approval/tests/test_phase51_handoff.py b/sports_federation_schedule_approval/tests/test_schedule_handoff.py
similarity index 91%
rename from sports_federation_schedule_approval/tests/test_phase51_handoff.py
rename to sports_federation_schedule_approval/tests/test_schedule_handoff.py
index ccb67c0..7ac6548 100644
--- a/sports_federation_schedule_approval/tests/test_phase51_handoff.py
+++ b/sports_federation_schedule_approval/tests/test_schedule_handoff.py
@@ -3,7 +3,7 @@ from odoo.tests.common import TransactionCase
 
 
 class TestPhase51ScheduleHandoff(TransactionCase):
-    """Regression checks for the operator-facing Phase 5.1 command boundary."""
+    """Regression checks for the operator-facing Schedule handoff command boundary."""
 
     def test_operator_actions_are_exposed_on_owning_models(self):
         schedule = self.env["federation.schedule"]
@@ -23,7 +23,7 @@ class TestPhase51ScheduleHandoff(TransactionCase):
         with self.assertRaises(ValidationError):
             self.env["federation.schedule.publication"].unlink()
 
-    def test_phase51_views_bind_buttons_to_model_actions(self):
+    def test_handoff_views_bind_buttons_to_model_actions(self):
         review_view = self.env.ref(
             "sports_federation_schedule_approval.review_form"
         ).arch_db
diff --git a/sports_federation_schedule_approval/views/menu.xml b/sports_federation_schedule_approval/views/menu.xml
index 79266ea..bc4cd2d 100644
--- a/sports_federation_schedule_approval/views/menu.xml
+++ b/sports_federation_schedule_approval/views/menu.xml
@@ -1,5 +1,5 @@
 <odoo>
-  <menuitem id="menu_schedule_review_queue" name="Schedule Review Queue" parent="sports_federation_competition_core.menu_competition_engine_v2_root" action="action_schedule_review_queue" sequence="60" groups="sports_federation_schedule_approval.group_schedule_approver,sports_federation_base.group_federation_manager"/>
+  <menuitem id="menu_schedule_review_queue" name="Schedule Review Queue" parent="sports_federation_competition_core.menu_competition_engine_competition_root" action="action_schedule_review_queue" sequence="60" groups="sports_federation_schedule_approval.group_schedule_approver,sports_federation_base.group_federation_manager"/>
   <menuitem id="menu_approved_schedule_queue" name="Approved Schedules" parent="sports_federation_base.menu_federation_publication" action="action_approved_schedule_queue" sequence="20" groups="sports_federation_schedule_approval.group_schedule_approver,sports_federation_base.group_federation_manager"/>
   <menuitem id="menu_schedule_publication_history" name="Schedule Publications" parent="sports_federation_base.menu_federation_publication" action="action_schedule_publication_history" sequence="30" groups="sports_federation_schedule_approval.group_schedule_approver,sports_federation_base.group_federation_manager"/>
 </odoo>
diff --git a/sports_federation_scheduling/views/menu.xml b/sports_federation_scheduling/views/menu.xml
index 78ac230..c98c1ac 100644
--- a/sports_federation_scheduling/views/menu.xml
+++ b/sports_federation_scheduling/views/menu.xml
@@ -1 +1 @@
-<odoo><menuitem id="menu_schedule_planner_v2" name="Schedule Planner" parent="sports_federation_competition_core.menu_competition_engine_v2_root" action="action_schedule_planner_v2" sequence="50"/></odoo>
+<odoo><menuitem id="menu_schedule_planner_competition" name="Schedule Planner" parent="sports_federation_competition_core.menu_competition_engine_competition_root" action="action_schedule_planner_competition" sequence="50"/></odoo>
diff --git a/sports_federation_scheduling/views/scheduling_views.xml b/sports_federation_scheduling/views/scheduling_views.xml
index c1e7c43..8bb8d9f 100644
--- a/sports_federation_scheduling/views/scheduling_views.xml
+++ b/sports_federation_scheduling/views/scheduling_views.xml
@@ -1,5 +1,5 @@
 <odoo>
  <record id="schedule_list" model="ir.ui.view"><field name="name">federation.schedule.list</field><field name="model">federation.schedule</field><field name="arch" type="xml"><list decoration-success="state == 'published'" decoration-info="state == 'ready_for_review'"><field name="name"/><field name="edition_id"/><field name="matchday_id"/><field name="structure_id"/><field name="revision"/><field name="state" widget="badge"/></list></field></record>
  <record id="schedule_form" model="ir.ui.view"><field name="name">federation.schedule.form</field><field name="model">federation.schedule</field><field name="arch" type="xml"><form><header><button name="action_open_submit_for_review" type="object" string="Submit for Review" class="btn-primary" invisible="state not in ('draft','changes_requested')"/><field name="state" widget="statusbar" statusbar_visible="draft,ready_for_review,changes_requested,approved,published"/></header><sheet><div class="alert alert-info" role="status" invisible="state != 'draft'">Assign every fixture, resolve blocking conflicts, then submit this schedule for independent review.</div><div class="alert alert-info" role="status" invisible="state != 'ready_for_review'">Submitted evidence is immutable and is waiting for an independent schedule approver.</div><div class="alert alert-warning" role="alert" invisible="state != 'changes_requested'">Changes were requested. Update the assignments, then submit a new review revision.</div><div class="alert alert-success" role="status" invisible="state != 'approved'">The schedule is approved and ready for publication by the approval team.</div><div class="alert alert-success" role="status" invisible="state != 'published'">This immutable publication is available to Match-Day Operations.</div><group><field name="name"/><field name="edition_id"/><field name="matchday_id" domain="[('edition_id','=',edition_id),('state','=','capacity_ready')]"/><field name="structure_id" domain="[('edition_id','=',edition_id),('state','=','frozen')]"/><field name="revision" readonly="1"/></group><notebook><page string="Assignments"><field name="assignment_ids"><list editable="bottom"><field name="fixture_id" domain="[('structure_id','=',parent.structure_id)]"/><field name="slot_id" domain="[('matchday_id','=',parent.matchday_id),('state','=','available')]"/><field name="method"/><field name="assigned_by_id" readonly="1"/></list></field></page><page string="Change History"><field name="change_ids"><list><field name="revision"/><field name="command"/><field name="fixture_id"/><field name="old_slot_id"/><field name="new_slot_id"/><field name="actor_id"/><field name="reason"/></list></field></page></notebook></sheet><chatter/></form></field></record>
- <record id="action_schedule_planner_v2" model="ir.actions.act_window"><field name="name">Schedule Planner</field><field name="res_model">federation.schedule</field><field name="view_mode">list,form</field></record>
+ <record id="action_schedule_planner_competition" model="ir.actions.act_window"><field name="name">Schedule Planner</field><field name="res_model">federation.schedule</field><field name="view_mode">list,form</field></record>
 </odoo>
diff --git a/sports_federation_standings/models/standing.py b/sports_federation_standings/models/standing.py
index bfa2191..c5902fc 100644
--- a/sports_federation_standings/models/standing.py
+++ b/sports_federation_standings/models/standing.py
@@ -330,11 +330,11 @@ class FederationStanding(models.Model):
             names=names,
             lot_seed=self.id,
         )
-        self._phase2_tiebreak_notes = notes
+        self._legacy_tiebreak_notes = notes
         return ranked
 
     def _compute_tiebreak_notes(self, sorted_items, participant_map):
-        notes = getattr(self, "_phase2_tiebreak_notes", None)
+        notes = getattr(self, "_legacy_tiebreak_notes", None)
         if notes is not None:
             return notes
         _participant_map, matches, names = self._ranking_context(dict(sorted_items))
diff --git a/sports_federation_standings/tests/test_standings_performance.py b/sports_federation_standings/tests/test_standings_performance.py
index a9fed17..22ec2f5 100644
--- a/sports_federation_standings/tests/test_standings_performance.py
+++ b/sports_federation_standings/tests/test_standings_performance.py
@@ -1,4 +1,4 @@
-"""Performance regression tests for standings computation (Phase 3, Item 5).
+"""Performance regression tests for standings computation (Integration, Item 5).
 
 Verifies that _build_standing_table() is O(n + m) — the number of ORM queries
 must not grow with the number of matches (they are fetched in one batch query).
diff --git a/sports_federation_standings/tests/test_tiebreak_notes.py b/sports_federation_standings/tests/test_tiebreak_notes.py
index 984ad2c..7473835 100644
--- a/sports_federation_standings/tests/test_tiebreak_notes.py
+++ b/sports_federation_standings/tests/test_tiebreak_notes.py
@@ -1,5 +1,5 @@
 """
-Tests for tiebreak notes on federation.standing.line (Phase 3).
+Tests for tiebreak notes on federation.standing.line (Integration).
 
 Coverage:
 - Teams with different points: no tiebreak note for lower team
